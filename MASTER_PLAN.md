# MASTER PLAN — ChessCoin
> **Этот файл — обязательное чтение в начале каждой сессии Claude.**
> До любых действий (даже до первого `Bash`/`Read`) — прочитать целиком,
> сверить «где остановились», и только потом отвечать пользователю.
> Правило закреплено в `CLAUDE.md` § «ПЕРЕД ЛЮБЫМ ДЕЙСТВИЕМ».

**Владелец продукта:** Кенан (профиль `SAM` на Windows).
**Продакшн:** `185.203.118.96` → `https://chesscoin.app` (проверка: `getent hosts chesscoin.app`).
**Инфра-сервер:** `185.203.116.131` — Claudia, LightRAG, прочее. ChessCoin сюда **не возвращать**.
**Канон требований:**
- `чат/GAME_REQUIREMENTS_FROM_KENAN.md` — старый канон до 25.04.2026 (47 элементов, 426 требований).
- **`чат/SPEC_2026-05-12_KENAN.md`** — НОВЫЙ канон от 12.05.2026 (6 подсистем).
  Где конфликтует со старым — побеждает этот.
- **`чат/REFERRAL_RANKS_SPEC.md`** — реферальная система (18 рангов, Q1–Q10
  для уточнения Кенаном).
**Хронология реплик Кенана:** `чат/KENAN_TIMELINE.md` (2 365 сообщений до 25.04.2026).

---

## 0. Главные законы

1. **Эталоны дизайна неприкосновенны.** HomePage и JARVIS Play Modal — шаблоны
   стилистики. Любая страница/модал = повторяет эти эталоны. Это требование Кенана
   `[2026-04-03 10:44:24]` и `[2026-04-03 16:08:44]`.

2. **Не упрощать одобренные компоненты.** Кенан несколько раз ловил Claude на
   «упрощениях» HomePage и BattlesPage. Цитата `[2026-04-25 09:54:47]`:
   *«ты блядь план строил усовершенствование системы визуала для будущего и снёс
   весь фронтенд — пришлось с нуля все дорисовывать»*. Правило: **если фича одобрена
   в этом MASTER_PLAN — её не трогать без явного запроса Кенана.**

3. **Один CoinIcon (золотой конь, viewBox 32×32).** Никаких «стопок монет», эмодзи
   `🪙` и т.п. Никаких других иконок монеты, кроме файла `frontend/src/components/ui/CoinIcon.tsx`.

4. **Эмодзи — минимум.** В UI вместо эмодзи — SVG-иконки из
   `frontend/src/components/icons/` (`ChessIcons.tsx`, `TournamentIcons.tsx`).
   Кенан явно требовал убрать эмодзи в карточках турниров `[2026-05-09]`.

5. **Один источник истины для модалов:** `useConfirm` для подтверждений, `Modal`
   для bottom-sheet. **Критические модалы (подтверждение, выход из режима, покупка) —
   по центру**, не bottom-sheet (`[2026-04-25 09:54:47]`).

6. **`window.innerWidth` запрещён** — только CSS media queries (правило в CLAUDE.md).

7. **Сделал → проверил → убедился → ответил.** Цитата Кенана `[2026-04-25 09:54:47]`:
   *«всегда только правда. Сделал — проверил — убедился — ответил? Сможешь?»*
   Перед `git push` — `docker logs --since 1m | grep -i error`, `curl -I
   https://chesscoin.app`, `getent hosts chesscoin.app`.

8. **Деплоить ТОЛЬКО на 118.96.** Перед `ssh` — `getent hosts chesscoin.app`
   должен ответить `185.203.118.96`. Если IP другой — обновить `CLAUDE.md` и
   этот файл, прежде чем продолжать. Урок 2026-05-11: ~20 коммитов улетели мимо
   prod, потому что в памяти числился старый сервер.

---

## 1. Что одобрено и работает (НЕ ТРОГАТЬ)

> Это — белый список. Менять только если Кенан явно сказал «переделай X».
> Перед изменением — открыть `GAME_REQUIREMENTS_FROM_KENAN.md` и убедиться,
> что трогаешь то, что Кенан просил тронуть.

### 1.1 HomePage
- Паспортная карточка: имя ≤14 знаков, баланс «3+1» (`12,5К`/`5,65М`/мин `1К`),
  ELO выделен жёлтым, аватар +20%, имя +2pt, ELO +3pt, флаг страны после имени.
- Активные сессии — панель видна только если есть незавершённые партии (макс 3).
- Блок J.A.R.V.I.S — один объединённый модал (Play Modal): уровень → 3 блока выбор
  цвета (Random по умолчанию) → 2 ряда таймов `1/3/5/15/30/60` → кнопка «Играть»
  → 3-сек отсчёт.
- Кнопка `+ попытки` рядом со звёздами → модал покупки попыток.
- Блоки режимов (Battle, War, Tournament, Puzzle, Lesson, Shop).
- Никаких «три точки/в сети» (убрано `[2026-04-03 11:27:51]`).
- Splash: иконка коня без синего фона (Кенан требовал `[2026-04-19 01:05:43]`).

### 1.2 BattleHistoryCard (единый шаблон карточек завершённых партий) — commit `8ac62fd`
- Два кликабельных аватара по краям (клик → `/profile/:id`)
- Бейдж результата ПОБЕДА/НИЧЬЯ/ПОРАЖЕНИЕ + цвет рамки (зелёный/синий/красный)
- Дата проведения, ставка, выигрыш
- Корона над аватаром победителя (фирменный SVG)
- Клик в центр карточки → открывает `PgnReplayModal`
- Используется на двух страницах: `BattlesPage` (вкладка ИСТОРИЯ) и `BattleHistoryPage`

### 1.3 GamePage action-bar — commit `fd252f0`
- Единый layout: 4 слота `Главная | Сохранить | (Ничья/Донат) | (Сдаться/Счётчик)`
- Игроки видят 3-й слот = «Ничья», 4-й = «Сдаться»
- Зрители видят 3-й слот = «Донат» (popup с 4 пресетами 100/1k/10k/100k),
  4-й = «Счётчик зрителей и просмотров» (тап = Браво)
- Доска и панели игроков **идентичны** для игрока и зрителя.

### 1.4 GamePage: персонализированный emit `"game"` — commit `0b65151`
- Backend в 9 точках использует `emitGameToRoom(io, sessionId, session)` —
  каждый сокет получает свой `formatSession(session, sockUserId)`, зрителям —
  `mySideId=null`.
- **Это исправляет известный баг переворачивания доски** после `game:join` —
  не возвращать общий `io.to(sessionId).emit("game", formatted)`.

### 1.5 TournamentsPage — фирменные иконки и confirm — commit `89048b5`
- Все эмодзи убраны, иконки `IcoWorld/IcoCountry/IcoSeasonal (лавровая ветвь)
  /IcoMonthly/IcoWeekly/IcoTrophy/IcoLeaderboard/IcoDonate/IcoSwords/IcoFlag`.
- Confirm перед вступлением: «Взнос идёт в призовой фонд и НЕ возвращается».
- Раскладка кнопок: главная Вступить/Выйти на всю ширину + две квадратные
  иконки 44×44 справа (Лидеры/Донат).
- InfoPopup — показывается **только в первой сессии** (Telegram CloudStorage +
  localStorage); кнопка `?` всё ещё открывает явно.

### 1.6 Турнирные сессии = публичные батлы — commit `9aca9ff`
- Backend создаёт сессию с `isPrivate=false`, эмитит `battles:added`.
- В лобби каждой турнирной партии — бейдж «🏆 ТУРНИР» (в Battle card).
- `BattleChallengeCard` показывает обоих игроков (если есть) и три варианта CTA:
  «⚔️ Принять» / «👁 Смотреть» / обычное «Войти».
- `joinBattleSession` защищён от 3-го игрока (`SESSION_FULL`).

### 1.7 Цикл турнира замкнут — commit `5bc17ee`
- `finishSession.updateTournamentMatch()` — после завершения сессии обновляет
  `TournamentMatch.status=FINISHED`, инкрементит `wins/losses/draws/points`.
- `ensureSystemTournaments` — закрывает стейл-периоды, дедуп дублей, cron каждый час.
- `matchmakeAllTournaments` — нормальные пары [0,1],[2,3],…, cron каждые 2 мин.
- Миграция `20260509_swiss_tournament_fields`: `deadline/points/buchholz` колонки.

### 1.8 Цикл войны структурирован — commits `0235a92` + `e3e83c1`
> ⚠️ **ВНИМАНИЕ:** распределение трофеев и логика возврата вклада в этом разделе
> УСТАРЕЛИ по канону `SPEC_2026-05-12_KENAN.md §3.E`:
> - Распределение: **20 % / 10 % / 5 % / 65 % + 10 % комиссия стола** (не «вес»).
> - При выходе из клана — вклад **ВОЗВРАЩАЕТСЯ** на личный счёт.
> Эти пункты нужно переделать в Фазе 3 (см. `REFERRAL_RANKS_SPEC.md §7`).

- **Backend:**
  - Главком — по `user.referralCount DESC`, при равенстве `joinedAt ASC`.
  - Список бойцов: при войне → по победам в этой войне → общие победы → алфавит;
    без войны → общие победы → алфавит; `?sort=alpha` форсит алфавит.
  - Вступление: 10 000 ᚙ → казна страны, `CountryMember.contribution=10000`.
  - Выход: взнос остаётся в казне.
  - Donate: `Country.treasury += amount` И `member.contribution += amount`.
  - При `finishWar`: казна победителя делится по весу `wins_in_war*1000 + contribution`,
    каждому → `COUNTRY_WAR_WIN` транзакция; казна → 0; ничья → ничего.
- **Frontend (`WarsPage` → `CountryModal`):**
  - Кнопка «Вступить» показывает цену `10 000 ᚙ`, confirm с предупреждением.
  - Бейдж «ГК» (золотой) у топ-1 по рефералам + строка «Главком · N реф.».
  - Карточка бойца: `W/L` цветом + `▲ N в войне` (при активной) + «вклад N ᚙ».
- **Миграция `20260510_country_wars_structure`:** `contribution BIGINT` колонка,
  `TransactionType.COUNTRY_ENTRY`, `COUNTRY_DONATION`, индекс по contribution.

### 1.9 Тёмная премиум-палитра
Все страницы (HomePage, NationsPage, TournamentsPage, ProfilePage, GamePage,
LessonPage, ExchangeTab, ShopPage) **переведены с CSS-переменных на хардкод**:
- Фоны `#141018`/`#0F0E18`, золото `#F0C85A`/`#D4A843`, зелень `#3DBA7A`,
  красный `#FF5B5B`, текст `#EAE2CC`/`#9A9490`/`#5A5248`.
- AdminPage пока с CSS-переменными (не пользовательская страница, низкий приоритет).

### 1.10 Инфраструктура
- `docker-compose.yml`: `POSTGRES_INITDB_ARGS=--auth-host=md5 --auth-local=trust` +
  `command: postgres -c password_encryption=md5` — фиксит pgbouncer ↔ postgres auth.
- На существующих БД нужен manual fix: `ALTER USER chesscoin PASSWORD ...` +
  `sed pg_hba.conf scram-sha-256 → md5`.

---

## 2. Что ещё НЕ сделано (открытый бэклог требований Кенана)

> **Источники:**
> - `чат/SPEC_2026-05-12_KENAN.md` — НОВЫЙ канон (имеет приоритет)
> - `чат/REFERRAL_RANKS_SPEC.md` — реферальные звания (18 рангов)
> - `чат/GAME_REQUIREMENTS_FROM_KENAN.md` § 5 + § 7 — старый канон
>
> Не начинать без явной команды Кенана. Перечень с приоритетом «как у Кенана
> в последнем сообщении».
>
> **NEW от 2026-05-12 (см. SPEC):** Jarvis 6 таймов, бот публикует посты в
> канал (топ-ставки час, итоги турниров), реф-ссылка батла привязывает в команду,
> макс 100 в клане, approve/reject/kick главкомом, без офицера нет войны,
> распределение войны 20/10/5/65 %, возврат вклада при выходе, касса чемпионата
> переходит между периодами, бейдж золотой лошади, темы магазина 10K–1M ᚙ,
> TonConnect 1 TON unlock + 0.5 % всех операций.

### A. Регрессии (Кенан назвал их и они ещё открыты)

- **A1. Иконка приложения на splash — старая синяя.** `[2026-04-25 09:54:47]`.
  Должна быть новая иконка коня без синего фона, увеличенная ×2.
- **A2. Модал «Активные сессии» с 3 мини-досками и таймерами** — не реализован.
  Описание: `[2026-04-03 11:27:51]`.
- **A3. Модал «Выход из страны» — должен быть по центру**, сейчас bottom-sheet.
  `[2026-04-25 09:54:47]`.
- **A4. Confirm перед выходом из активной партии** — Кенан настаивает что был.
  Восстановить, если откатил.
- **A5. Кликабельность аватаров и центрирование текста под аватаром** во всех
  карточках batlle/lobby/list. Частично сделано, докатить везде.
- **A6. Старые «стопки монет»** — пройти весь UI, заменить на единый `CoinIcon`.

### B. Новые системы

- **B1. TonConnect: 1 TON единоразово за подключение кошелька.** В коде есть
  `WALLET_UNLOCK` в `TransactionType`, логика отсутствует. `[2026-04-25]` решено.
- **B2. Withdraw TON.** Заблокирован в v6. Кенан хочет возобновление, комиссия
  0.5%. (`GAME_REQUIREMENTS_FROM_KENAN.md` §5 Q5).
- **B3. Подтверждение главкома при вступлении в страну (approve/reject).**
  `[2026-04-25 09:54:47]` — в коде open-join, нужно ввести approve-flow.
- **B4. Авто-смена главкома** при бездействии **7 дней** — `[2026-04-25]` решено.
  Сейчас он определяется по референс-числу, а должен периодически
  пересматриваться (cron + `lastSeenAt` или подобное).
- **B5. Социальные таски с бонусами 3k/5k/10k/20k/50k/100k** за 3/5/10/20/50/100
  рефералов — `[2026-04-03 11:27:51]`.
- **B6. Lesson этаж: open-by-progress** — частично сделано, дочинить блокировку
  уровней до прохождения предыдущего.
- **B7. ELO под аватаром в каждой карточке, центрирован, на 1 тон светлее** —
  `[2026-04-06 08:52:32]`. Частично сделано, привести к единому виду.

### C. Стандартизация (UX)

- **C1. Заголовки страниц** — единый шрифт/размер/позиция как на странице «Войны».
  `[2026-04-04 11:42:22]`.
- **C2. Toasts vs красные полоски** — все «нельзя играть/нужно купить» как
  toast 3 сек, не «мигающая полоса». `[2026-04-19 02:18:13]`.
- **C3. JarvisModal рaward 1k..1M** — синхронизировать с `config.ts:84-105`,
  убрать устаревшие числа.
- **C4. i18n: чистый ru/en, никаких смешений** — `[2026-03-30 22:43:45]`.
  Проверить TournamentsPage, WarsPage (там сейчас русский хардкод вперемешку с
  `t.wars.*`).

---

## 3. История ошибок Claude (то, что Кенан помнит)

> Не повторять. Если ловлю себя на одной из этих линий — стоп и спросить Кенана.

1. **Снёс фронтенд переделкой стилей** — пришлось с нуля дорисовывать (200$ свыше
   плана). `[2026-04-25 09:54:47]`.
2. **Откатывал то, что уже починил** — `[2026-04-25 09:59:57]`:
   *«если работает надо дочинить, а ты сейчас еще пару вещей испортишь типа откатывал»*.
3. **Снёс вкладыши Публичный/Приватный в BattlesPage** — `[2026-04-25 09:54:47]`.
4. **«Сделал» без проверки** — упорно сообщал что починено, без curl/screenshot.
   `[2026-04-25 09:30:05]`.
5. **`useSocket` stale closure → разрушение лобби** — выкатывал по неосторожности.
6. **TonConnect — не разобрался в материалах** и сломал и десктоп, и мобильный.
   `[2026-04-25 09:54:47]`.
7. **Старая синяя иконка на splash** — Клод что-то откатил при чинке `useAuth`,
   иконка вернулась. `[2026-04-25 09:54:47]`.
8. **(2026-05-11) Деплоил 20 коммитов на legacy-сервер.** Не проверил
   `getent hosts chesscoin.app` → попадал на `185.203.116.131` вместо `118.96`.
   **Канон-фикс:** теперь обязательная проверка резолва домена перед `ssh`.
9. **(2026-05-09) Дублирующийся `case 'COUNTRY'` в `getCurrentPeriod`** — оставил
   мусор после фикса. Зафиксил в `75e6568`.
10. **Меняла «лёгкие» баги массовыми правками файлов** — Кенан несколько раз
    замечал, что один файл правлю «то одно, то другое».

---

## 4. Что было сделано в последней сессии (2026-05-09 → 2026-05-12)

> Перечисление по коммитам. Это **одобрено** (Кенан не возражал к 12.05) и в проде.

| Commit | Описание | Статус |
|---|---|---|
| `8f8be91` | CLAUDE.md: два сервера PROD/INFRA | ✓ |
| `e3e83c1` | UI цикла войны (confirm join 10k, бейдж ГК, currentWarWins) | ✓ задеплоено на prod |
| `0235a92` | Backend цикла войны (схема, finishWar, getCommander, sort) | ✓ задеплоено |
| `0b65151` | Персонализированный `emitGameToRoom` (доска не переворачивается) | ✓ задеплоено |
| `5bc17ee` | Турниры: `updateTournamentMatch`, dedupe, matchmake cron 2 мин | ✓ задеплоено |
| `fd252f0` | Единая action-bar игрок/зритель + donate popup | ✓ задеплоено |
| `8ac62fd` | `BattleHistoryCard` — единый шаблон | ✓ задеплоено |
| `89048b5` | Турниры: фирменные иконки, persistent infopopup, confirm | ✓ задеплоено |
| `9aca9ff` | Турнирные = публичные батлы в лобби | ✓ задеплоено |
| `42789ec` | postgres patches `pg_hba.conf` при старте | ✓ |
| `75e6568` | Удалён дубль `case 'COUNTRY'` | ✓ |
| `f2325e6` | Socket tournament:match → `getSocket()` + navigate | ✓ |
| `dcc36b3` | `ActiveMatch.tournamentId` + lookup | ✓ |
| `435fb17` | TournamentsPage: статус-бейдж, CTA «Играть матч» | ✓ |
| `050ffea` | postgres md5 auth для pgbouncer | ✓ |
| `00d16d9..b6cf3ac` | Premium dark на всех страницах | ✓ |
| `b58fc4c..2812ba1` | Доска, иконки, BattleCard унификация | ✓ |

**Изменения в БД prod (применено):**
- `tournament_matches.deadline / points / buchholz` + индекс
- `country_members.contribution` + индекс
- `TransactionType.COUNTRY_ENTRY` и `COUNTRY_DONATION`

**Очищено на legacy (`185.203.116.131`):**
- 8 контейнеров `chesscoin_*`, volumes, образы (4 шт, ~1.9 GB), `/opt/chesscoin`
- Бэкап БД: `/root/backups/chesscoin-legacy-final/db-20260512-021139.sql`
- **Не тронуто:** Claudia, LightRAG, translator, azerobbot, illuminant.

---

## 5. Возможные ошибки последней сессии (потенциальные проблемы)

> Это **гипотезы**. Кенан проверит и скажет что реально криво.
> Не ремонтировать ничего без явного «исправь X».

1. **Удалены ли где-то вкладыши Публичные/Приватные/История?** Кенан жаловался
   `[2026-04-25]` что Клод снёс именно эти вкладыши. В коммите `fd252f0` я
   переделала action-bar, в `8ac62fd` — карточку истории. Нужно проверить что
   фильтры на BattlesPage всё ещё на месте.

2. **Confirm перед выходом из активной партии** — есть ли он сейчас? Я в этой
   сессии его не добавляла и не удаляла. Если откатил во время фиксов GamePage —
   надо восстановить.

3. **Модал «Выход из страны» — по центру или внизу?** Я добавила confirm через
   `useConfirm` (центр). Но в `WarsPage` ещё есть `showLeaveConfirm` через
   bottom-sheet (`Set… leave`). Возможно — два места выхода, одно осталось внизу.

4. **CountryEntryFee на frontend = 10 000n хардкод** — а backend отдаёт `entryFee`
   в ответе. Если поменяем цену на сервере, фронт не обновится. Использовать
   только server-side значение.

5. **Эмодзи в `WarsPage`** — я заменила в `TournamentsPage`, но `WarsPage`
   ещё может содержать `🚪`, `⚔️`, `🏴` и т.п. Проверить.

6. **`donateAmt` тип Number → String** — может ломать поле ввода (`type="number"`
   ожидает Number, я передаю String). Frontend `<input type="number" value={...}>`.

7. **Pgbouncer auth fix применён руками на проде** (`docker exec ... ALTER USER`).
   Не reset-safe — переживёт ли pull-обновление postgres? Compose-патч есть, но он
   работает только при init на пустой БД.

8. **Wars `myMembership.contribution` отображается как BigInt-строка**, но если в
   старых записях `contribution` ещё 0 — не отрисую «вклад N ᚙ». Корректно? Думаю да.

---

## 6. Карта ключевых файлов проекта

| Тема | Backend | Frontend |
|---|---|---|
| Война | `backend/src/routes/wars.ts`, `backend/src/services/game/warMatchmaking.ts` | `frontend/src/pages/WarsPage.tsx` |
| Турниры | `backend/src/routes/tournaments.ts`, `tournamentSwiss.ts` | `frontend/src/pages/TournamentsPage.tsx`, `components/icons/TournamentIcons.tsx` |
| Игра | `backend/src/services/game/{socket.ts, finish.ts, format.ts, session.ts, timer.ts}` | `frontend/src/pages/GamePage.tsx`, `components/game/{ChessBoard, GameResultModal,…}.tsx` |
| Батлы лобби | `backend/src/services/game/{socket.ts, format.ts}` | `frontend/src/pages/BattlesPage.tsx`, `components/battle/BattleHistoryCard.tsx` |
| Экономика | `backend/src/services/economy.ts`, `routes/exchange.ts` | `frontend/src/pages/{ShopPage, ExchangeTab}.tsx` |
| Auth/Telegram | `backend/src/middleware/auth.ts`, `frontend/src/hooks/useAuth.ts` | `frontend/src/App.tsx` |
| Дизайн (общие) | — | `components/ui/{Modal, ConfirmModal, Button, Avatar, …}.tsx`, `components/layout/PageLayout.tsx`, `index.css` |
| Иконки | — | `components/icons/{ChessIcons, TournamentIcons}.tsx`, `components/ui/CoinIcon.tsx` |
| Эталоны | — | (в `chesscoin-archive`, не править) `HOMEPAGE_TEMPLATE.tsx`, `JARVIS_PLAY_MODAL_TEMPLATE.tsx` |

---

## 7. Чек-лист в начале каждой сессии

1. Прочитать **этот файл** (`MASTER_PLAN.md`) целиком.
2. Прочитать `CLAUDE.md` (свежие правила и закон серверов).
3. `getent hosts chesscoin.app` — проверить что prod-IP не сменился.
4. `git log --oneline -10` в worktree — что было сделано последним.
5. На проде: `ssh root@185.203.118.96 "cd /opt/chesscoin && git log --oneline -3"` —
   убедиться, что prod = main, не отстаёт.
6. Только после этого читать запрос Кенана и **уточнить, если непонятно** —
   не предполагать.

## 8. Чек-лист перед `git push`

1. Изменения покрыты **только тем, что просил Кенан** в текущей сессии (не
   «заодно» переделывать соседние файлы).
2. `git status` — нет случайных файлов в индексе (например, чужой `.log`).
3. Backend: `docker logs chesscoin_backend --since 1m | grep -iE 'error|fatal'` —
   пусто.
4. Frontend: пересобран и контейнер `chesscoin_frontend` Up.
5. `curl -sS -I https://chesscoin.app/ | grep last-modified` — обновился (или
   `index-*.js` хеш сменился).
6. Commit-message — конкретно что и зачем, без пафоса.

---

## 9. Записать в LightRAG после крупных решений

После каждого закрытого пункта из § 1 (одобренный набор) — `save_to_memory()` в
LightRAG `chesscoin_rag_secret_2026` через `http://185.203.116.131:9621/documents/text`
с полями `{"text": "...", "file_source": "MASTER_PLAN.md commit XYZ"}`.

В случае ошибки `body parse` — пропустить и записать в `MEMORY.md` (`C:\Users\SAM\
.claude\projects\C--Users-SAM-Desktop-chesscoin\memory\MEMORY.md`).
