# ChessCoin — каноническая спецификация (на основе всех реплик Кенана)

> **Источник:** `KENAN_TIMELINE.md` (2 365 реплик Кенана, 2026-03-21 → 2026-04-25).
> Кросс-сверка с текущим кодом: `GAME_DESIGN_KNOWLEDGE.md` (snapshot v7.1.0).
> **Правило отбора:** последнее по timestamp явное мнение по элементу = канон.
> Промежуточные формулировки — в «История». Если по элементу Кенан не высказывался —
> не включён (см. § 5 Открытые вопросы).

> **Метод сбора:** stream-парс файла, классификация реплик (req/bug/neg/disc),
> группировка по 47 ключевым элементам, сортировка по дате убыв. Шумовые preamble
> «This session is being continued…» отфильтрованы.

---

## 1. Глобальные принципы (стиль и архитектура)

### 1.1 Архив ↔ рабочая копия (зафиксировано)
- Архив `chesscoin-archive\` неприкосновенен; рабочая `chesscoin\` — единственное место правок;
  push в `kentavr34/chesscoin`, merge в `main` — только Кенан.
- При поломке рабочей копии — переименовать в `chesscoin-broken-YYYY-MM-DD`,
  взять новую из архива.

### 1.2 Эталоны дизайна (НЕПРИКОСНОВЕННЫ)
- `[2026-04-03 10:44:24]` — «Стилистика этой страницы будет отныне эталоном и шаблоном
  для коррекции и создания других страниц/модалов — сохрани эту страницу отдельно
  как шаблон». Эталон HomePage сохранён в `.claude/archive/HOMEPAGE_TEMPLATE.tsx`.
- `[2026-04-03 16:08:44]` — «окно создания партии для игры с джарвисом тоже сохраняем,
  как отдельную шаблонную форму». Эталон → `.claude/archive/JARVIS_PLAY_MODAL_TEMPLATE.tsx`.
- **CANONICAL:** все новые/правленные страницы/модалы повторяют стиль HomePage и
  JarvisPlayModal. Любые отклонения — нарушение.

### 1.3 Заголовки страниц (стандарт)
- `[2026-04-04 11:42:22]` (последнее) — «на всех страницах есть заголовок страницы.
  Шрифт один и тот же, но расположение и размер разные. Сделать стандартный заголовок —
  размер и шрифт как на странице Войны».
- **CANONICAL:** один шрифт + один размер + одна позиция заголовка во всех страницах,
  по образцу Войн.

### 1.4 Симметрия и отступы
- `[2026-03-30 22:30:50]` — «Визуальный стиль — симметрия, аккуратность страниц, отступы,
  шрифты, спец-уведомления — это пока очень слабо и не системно как в лучших аппах».
- **CANONICAL:** симметрия как принцип; `[2026-04-03 19:35:50]` — все аватары/панели
  выравниваются строго по центру/осям. `window.innerWidth` запрещён, только CSS media queries.

### 1.5 Многоязычность (i18n)
- `[2026-03-30 22:43:45]` — «все названия кнопок, страниц, подсказок, уведомлений,
  инструкций, званий, приветственного текста в боте — везде менять язык при смене языка
  из настоек в профиле. Пока 2 языка — русский и английский. **Недопустимо**, чтобы
  где-то даже в статусе игрока или в его звании использовались слова и буквы иного языка».
- **CANONICAL:** ru/en — 100% покрытие, никаких смешанных языков (включая бот).
  В коде уже расширено до 9 языков (`bot/i18n.py`), но требование Кенана = минимум ru+en
  без смешения.
- `[2026-04-03 20:17:36]` — «у Джарвиса звание пишется на английском, на главной
  странице — на русском. Лучше на одном языке писать», → использовать локализованные
  строки `t.jarvis.levels[idx].name` и `t.referrals.ranks[idx]`.

### 1.6 Тон коммуникации (бот, уведомления)
- `[2026-04-19 02:18:13]` — «уведомление должно выходить уже на этапе главной страницы
  при клике на Джарвис, не красная полоска на одну секунду, а всплывающее уведомление
  на ~3 секунды».
- **CANONICAL:** информационные уведомления — toast 3 сек, не «мигающая полоса».

### 1.7 Символ монеты (CoinIcon)
- `[2026-04-19 00:43:53]` (last) — «иконка золотой лошадки при запуске игры — заменяем
  на новую выданную ими, она же будет отображением монет в игре во всех местах. Никаких
  других значков стопки монет и прочего».
- `[2026-04-19 00:52:24]` — «меняй везде где есть символ монеты, меняй на заставке,
  или слова "монет" означает что рядом должен быть знак этой монеты».
- **CANONICAL:** одна иконка (золотой конь, viewBox 32×32) везде где упоминается
  «монета»/баланс. Никаких альтернативных «стопок».

### 1.8 Иконка приложения / splash
- `[2026-04-19 01:05:43]` — «иконка при заставке выходит на старом синем фоне —
  убрать этот фон, иконку лошади увеличить в 2 раза».
- `[2026-04-25 09:54:47]` — «опять стоит старая синяя идиотская иконка». **Эта проблема
  ещё не закрыта на 25 апреля.** Канон = новая иконка, без синего фона.

---

## 2. Страницы

### 2.1 HomePage

**Канонический список элементов** (`[2026-04-03 11:27:51]` + `[2026-04-03 10:34:41]`,
+ `[2026-04-01 20:28:11]` (что должно было быть): кнопка «Играть против Jarvis», блоки
режимов, достижения в профиле, баланс, переход в магазин, обучение):

1. **Паспортная карточка** (Аватар + ник + ELO + звание + страна-флаг):
   - Имя — до **14 первых знаков** (`[2026-04-03 10:34:41]`); если длиннее — обрезается.
   - Баланс — формат «3 цифры + 1 буква»: `12,5К`, `5,65М`, минимум `1К` (там же).
   - «Звание» и «Уровень Jarvis» — отображаются в окне одинаковой длины,
     +3 единицы шрифта.
   - Аватар +20%, имя +2 единицы шрифта, ELO +3 единицы, ELO выделен **жёлтым**.
   - После имени — место для флага страны (появляется когда игрок вступил в команду
     страны). История: `[2026-04-03 14:14:18]` — «значок прилип, проблема с флагом».
   - Убрать «три точки» и текст «в сети» слева (`[2026-04-03 11:27:51]`).
2. **Активные сессии** — панель показывается только если есть незавершённые партии
   (макс 3) (`[2026-04-03 10:16:36]`). Дизайн как панель «ежедневные задания».
   - Клик по панели → отдельный модал/страница активных сессий, 3 мини-доски с таймером,
     аватарами соперников, цветами фигур и зелёной надписью «ваш ход» у того, чей ход
     (`[2026-04-03 11:27:51]`). На 04-25 этот модал ещё не реализован
     (`[2026-04-25 09:54:47]` — «активные сессии нет модала»).
3. **Блок J.A.R.V.I.S** — клик открывает **один** модал (объединённый — не два):
   на верхней строке текущий уровень, ниже 3 блока выбор цвета (Random по умолчанию),
   ниже 2 ряда выбора времени `1, 3, 5, 15, 30, 60` минут, ниже кнопка «Играть» →
   3-сек таймер обратного отсчёта → доска (`[2026-04-03 11:27:51]`,
   `[2026-04-01 20:28:11]` — «давно убрали отдельный JarvisModal с уровнями»).
4. **Кнопка `+ попытки`** (рядом со звёздами) → модал покупки попыток:
   - Сверху — текущее количество звёзд, справа — баланс.
   - В середине — выбор количества к покупке (макс 3, в зависимости от текущих).
   - Кнопка «Купить» → списание + начисление звёзд (`[2026-04-03 11:27:51]`).
5. **Квесты ›** — переход на страницу заданий с **3 этажами** (см. § 4.5).
6. **Блоки режимов** — кнопки в режимы Battle, War, Tournament, Puzzle, Lesson, Shop.
7. **Splash/auth** — iconка лошади, без старого синего фона (см. 1.8).

**Запрет:** НЕ упрощать HomePage (`[2026-04-01 20:28:11]` — «где блоки режимов, где
достижения, где красивая главная»). Не использовать «другую» iconку — только новую CoinIcon.

### 2.2 BattlesPage

`[2026-04-04 13:16:36]` (создание-стандарт) + `[2026-04-19 02:30:57]`,
`[2026-04-19 02:48:54]`, `[2026-04-21 22:00:42]` (последние крупные правки).

**3 этапа отображения батла** (один шаблон карточки, меняется состояние):
- **Этап 1 — Вызов** (никто не принял): маленькое окошко-панель с минимум данных
  создателя (аватар + имя + ставка + минуты).
- **Этап 2 — Принят соперником / ожидание старта** (индикация противника).
- **Этап 3 — Live (идёт игра)**: красный/жёлтый индикатор статуса, таймер,
  + публичные дополнения (см. ниже).

**Делитель карточки на 3 вертикальных подуровня** (`[2026-04-19 02:30:57]`):
- Левая сторона: аватар + имя + ELO создателя; ник и ELO **строго под аватаром, по центру**.
- Правая сторона: аватар противника, или знак «случайный» в режиме открытого вызова.
- Середина: статус (`Ожидание`/`Live`), таймер длительности, ставка (центрирована).
- Знаки выбора цвета: «полосатая фигура половина чёрная/половина белая» = Random,
  иначе сплошной цвет; под фигурой — подпись `Чёрный/Белый/Random`.

**Сортировка списка публичных:** `[2026-04-19 02:48:54]` — Live партии вверху;
далее по фильтру убыв. ставка.

**Таймы для создания батла:** `[2026-04-04 13:40:13]` — `1, 3, 5, 15, 30, 60` минут
в 2 ряда (как на JarvisPlayModal). Не «свои» наборы.

**Шрифт заголовка `Батлы`:** `[2026-04-04 11:12:31]` — корпоративный (Inter), не Unbounded/Shift.

**Поведение при недостаточности средств / отсутствии попыток** (`[2026-04-04 23:52:18]`,
`[2026-04-04 13:52:26]`):
- Не показывать тост-полоску. Кнопка «Создать Батл» становится КРАСНОЙ с текстом:
  - «Нет попыток» → клик → модал покупки попыток поверх (overlay).
  - «Недостаточно монет» → клик → модал покупки монет / переход в магазин.
- После покупки — возврат на ту же страницу создания.

**Публичные vs приватные** (`[2026-04-21 22:00:42]` — финал):
- **Публичные:** счётчик зрителей лайв + общее число просмотров + касса (банк) +
  кнопка «Донат» рядом — болельщики могут пополнять кассу. Победитель забирает всю кассу;
  10% — комиссия стола.
- **Приватные:** только касса = взнос обоих игроков. Победитель забирает всё минус 10%.

**Вкладыши `Вызов / Сражение / История`** — `[2026-04-24 04:35:50]`: они **не вкладыши,
а этапы одной партии**. Решение Кенана: убрать как вкладыши, использовать как индикаторы
состояния карточки. `[2026-04-25 09:54:47]` подтверждает: «эти вкладыши почему убрал
дурень? Я тебя инструктировал про показатели лайв в публичных батлах». **CANONICAL:**
вкладыши `Публичные / Приватные / История` остаются как фильтр; «Вызов/Сражение/История»
как разделы — НЕ нужны.

**Клик по аватару игрока в карточке:** `[2026-04-05 19:25:04]` (явный bug-fix):
динамический id, переход на профиль того игрока, чей аватар нажали.

**На 25 апреля проблемы (открытые):**
- `[2026-04-25 09:54:47]` — кнопка «Вступить в клан» теперь видна, но модал «Выйти из
  страны» — внизу, должен быть **по центру**.
- Кликабельность аватаров и текст под аватаром по центру — **не сделано**.

### 2.3 GamePage (доска)

Игровая доска для всех режимов имеет **первый шаблон** (без счётчика зрителей/кассы)
и **второй шаблон** для публичных батлов / войн (со счётчиком зрителей, кассой, донатом).

`[2026-04-06 08:48:44]`:
- Таймер времени +20%, цвет — основной тематический (золотисто-жёлтый), не серо-чёрный.
- Полоса игрока ровно посередине между нижним краем доски и верхним краем нижней
  панели (~ −3 мм от текущей).

`[2026-04-19 01:25:53]` + `[2026-04-19 01:40:29]` + `[2026-04-19 01:47:19]`:
- **Аватары обоих игроков** — на левой стороне, по одной линии, с ограничением длины имени.
- **Флаг страны** — отображается рядом с именем, если игрок вступил в страну (например `🇦🇿`).
- **Полоса игрока** (ободок выделения) — у обоих сразу, не меняется по очереди хода.
  Меняется только подсветка часов (у того, чей ход). Полоса +10% вниз и +10% вверх.
- ELO/звание — +10% к шрифту.
- Фон полосы — лёгкий жёлтоватый градиент.

`[2026-04-03 21:36:50]` (ничья):
- При нажатии «Ничья» уведомление сверху по центру: «Ваш запрос ничьи отправлен сопернику».
  Соперник получает кнопки `Согласен / Не согласен`. По таймауту — не отправляется ход
  партии «играй дальше».

`[2026-04-25 09:54:47]`:
- **Confirm перед выходом из активной партии** — Кенан считает что это уже было сделано;
  должно быть. Если откат произошёл — восстановить.

### 2.4 NationsPage / Войны (см. также §3.3)

`[2026-04-25 09:54:47]` — финальный список открытых требований:
- **Модал выхода из страны** — должен быть **по центру**, не внизу.
- **Уведомление при выходе/входе** — присутствует.
- **Логика донатов при вступлении и при выходе** — должна быть (детали в §3.3).
- **Главнокомандующий подтверждает или отклоняет** новых членов страны
  (см. также §3.3 — открытый вопрос «модерация состава»).
- **Если главнокомандующий не подтверждает долго** — авто-смена через N дней
  (см. §3.3 — N не определён, открытый вопрос).
- **Логика вызова войны и отправка батлов всем игрокам по 10 лайв за 1 раз** — см. §3.3.

`[2026-04-03 14:14:18]` — флаг страны видно в паспортной карточке, проблема с местом —
оставить место под флаг.

### 2.5 TournamentsPage

`[2026-03-30 22:30:50]` (канонический список типов):
Чемпион **недели / месяца / сезона / страны / мира**. Система генерирует чемпионаты
по времени. Победитель — победивший к моменту истечения. Вступившие получают призыв
в раздел Battles в приватные. Не принявший вызов в течение **24 часов проигрывает**
(автопоражение).

`[2026-04-21 22:29:38]`:
- Доделать модалы войн и турниров.
- Система автоматического формирования публичных батлов внутри турнира.
- Кликабельность кнопок при открытии.

`[2026-04-21 22:22:17]`:
- Из статистики **убрать игру с ботом как процесс** (т.е. боты не считаются как
  турнирные/войсковые партии).

### 2.6 ShopPage

`[2026-03-30 22:30:50]` — «Магазин и темы, аватары и звуки — купил за монеты — и тут же
установил — вышло». Принцип: **мгновенная установка** после покупки.
`[2026-04-04 21:56:56]`:
- Если на принимаемой партии не хватает попыток — модал покупки попыток.
- Если не хватает монет — модал «нет монет», отправляющий в магазин/покупку монет.

`[2026-04-01 12:48:44]` — «магазин не работал, покупаешь — внедрялись, но не
работали» — требование: реальная работоспособность.

### 2.7 ExchangeTab (биржа TON↔ᚙ)

`[2026-03-30 22:30:50]`:
- Цены/ордера/исполнение — биржа покупки/продажи монет за TON.
- **Комиссия системы 0.5%** со всех торгов.
- **1 TON единоразово** за подключение кошелька.
- Все наши заработки → на наш кошелёк; торговля между игроками — TON между ними,
  но 0.5% с каждой операции — нам.

В текущем коде (`exchange.ts`): 0.5% уже реализовано; «1 TON за подключение кошелька» —
**не найдено**, открытый вопрос (см. §5).

### 2.8 ReferralsPage

`[2026-03-30 22:30:50]` — «реферальную систему перенести в режим Войн» (см. §3.3 —
звание зависит от рефералов, главком определяется по званию). Сама вкладка остаётся
как страница инвайта/статистики. `[2026-04-01 12:48:44]` — «надо было делать лучшую
реферальную систему».

### 2.9 TasksPage / Задания

`[2026-04-03 11:27:51]` — **3 этажа** (см. §4.5):
1. Ежедневные (5 в день из Lichess);
2. Обучение (последовательно открываются);
3. Социальные (подписки, рефералы — задаются админом).

### 2.10 ProfilePage

`[2026-04-21 22:29:38]`:
- При клике на аватар игрока в любой карточке — переход на его профиль (read-only вид
  чужого профиля). Уже частично починено в `[2026-04-05 19:25:04]`.
- В профиле — звания, бейджи, история.

### 2.11 LeaderboardPage / Рейтинги

`[2026-04-06 08:52:32]`:
- Расчёт ELO — «как во всех шахматных клубах и на турнирах» (классический Эло, K=32 уже
  в коде).
- Уровень ELO **в каждой карточке игрока** строго под аватаром, центровка по середине
  аватара, на 1 тон светлее.

### 2.12 BattleHistoryPage

`[2026-04-21 22:22:17]`:
- Из статистики **убрать игру с ботом** как процесс.
- PGN/ПДЖИН — вид партии поднять выше, центрировать в окне, навигация удобная и
  «без жадло» (без скучивания).
- `[2026-04-21 22:29:38]` — «дизайн ПДЖН — в списке должны быть аватары игроков,
  при клике на аватар — переход на профиль того игрока».
- `[2026-04-05 00:44:06]` — все батлы должны попадать в историю (сейчас попадает
  только история Jarvis). Иконка «крестик» сверху карточки — убрать.

### 2.13 SettingsPage

`[2026-03-30 22:43:45]` — переключатель языка интерфейса (пока ru/en); звуки;
тема. По коду — `User.activeTheme` есть, переключатель темы — присутствует.

### 2.14 PuzzleDailyPage / Ежедневные задачи

`[2026-04-03 11:27:51]` (этаж 1 заданий) — `5 задач/день` из Lichess, рандом, без
подсказок, награда `1 000 — 20 000` ᚙ за каждую в зависимости от сложности; решённые
не выдаются повторно. Скрипт качает по `200/мес` (`[2026-03-30 22:30:50]`).

### 2.15 LessonPage / Обучение

`[2026-04-03 11:27:51]` (этаж 2 заданий):
- Загружаются из Lichess **один раз**, сортируются по сложности (1..N).
- Лимит **5 заданий в день**.
- Уровни закрыты — открываются по очереди (только пройдя предыдущий).
- Награда: `1000 + 1000 × level` (т.е. на уровне 100 — 100 000 ᚙ).

### 2.16 AdminPage

`[2026-04-03 11:27:51]` (этаж 3 заданий):
- Создание **социальных заданий** из админки или команд бота:
  - Подписаться на TG-канал → авто-проверка → бонус.
  - Подписаться на Instagram / YouTube → бонус.
  - Пригласить N друзей: 3, 5, 10, 20, 50, 100 → 3 000 / 5 000 / 10 000 / 20 000 /
    50 000 / 100 000 ᚙ соответственно (помимо обычных реферальных).

### 2.17 TransactionHistoryPage

Кенан явно не описывал. Использовать существующее поведение из кода.

---

## 3. Режимы

### 3.1 BATTLE 1v1
См. §2.2. Дополнительно:
- `[2026-04-04 13:11:59]` — «Баттл на 10 000 монет, был сыгран — должен быть в истории».
  Все категории: батлы, войны, кубки — в общей истории.

### 3.2 BOT (JARVIS)
- `[2026-03-30 22:30:50]` — «чем выше уровень, тем выше награда. Даже при поражении
  игрок получает монеты за выигранные фигуры».
- 20 уровней (см. в коде, `config.ts:84-105` — действующие награды 1k..1M).
- Доступ: только до уровня `jarvisLevel + 1`.
- `[2026-04-19 02:18:13]` — нельзя запускать вторую партию с Jarvis при наличии активной;
  предупреждение должно появляться **на главной странице** при клике на Jarvis (не
  после открытия модала).
- `[2026-04-03 17:55:21]` — после нуля попыток при попытке играть — корректный модал
  покупки, не «красная полоска как будто система сломалась».

### 3.3 WAR (войны стран)

`[2026-03-30 22:30:50]` (большой канон) + `[2026-03-22 16:03:57]` (10 партий за раз)
+ `[2026-04-25 09:54:47]` (открытые мелочи):

**Состав:**
- Список стран (30 в seed). Игрок выбирает страну → вступает в её сборную (один игрок =
  одна страна). `[2026-04-25 09:54:47]` — главнокомандующий **подтверждает/отклоняет**
  заявку (в коде сейчас открытый join — нужно добавить approve flow).
- При активной войне страну покинуть нельзя; сменить страну тоже нельзя.

**Главнокомандующий:**
- Назначается по **самому высокому званию** (звание = от числа активированных рефералов).
  → реферальная система **перенесена в режим войн** (`[2026-03-30 22:30:50]`).
- Если он «не отвечает на запросы более недели» — власть переходит игроку с самым
  высоким рейтингом побед.
- `[2026-04-25 10:13:46]` — «N дней бездействия для авто-смены — сколько?» —
  **открытый вопрос Кенана к самому себе**, см. §5. Текущая интерпретация: 7 дней
  (`более недели`).

**Объявление войны:**
- Только главнокомандующий объявляет войну другой стране.
- Нельзя на самих себя; нельзя если кто-то из сторон уже в войне (в коде это есть).

**Матчмейкинг:**
- Система выдаёт **не более 10 партий одновременно** между бойцами стран
  (`[2026-03-22 16:03:57]`).
- Партии идут к разным игрокам — пока все не получат шанс. После закрытия одной —
  следующая. Это мотивирует страну подбирать сильных бойцов.
- 1 игрок может побить «армию из 1000» — учитываются **тотал-побед** в течение войны.
- Призыв приходит в раздел Batles в **приватные**, ожидает принятия. После принятия —
  партия запускается и попадает в **публичные**, все могут смотреть зрителями.

**Финал** — `[2026-07-31]`, Кенан утвердил распределение целиком:

- Побеждает страна, чьи бойцы набрали больше побед до истечения времени войны.
- Победившая забирает казну проигравшей **целиком, до нуля**. Своя казна
  остаётся при ней. Прежнее ограничение `min(казна победителя, казна
  проигравшего)` отменено.
- Захваченное делится между своими воинами **строго кратно числу побед**:
  одна победа = один эквивалент, доля = касса × победы бойца ÷ сумма побед всех.
  Прежние 20/10/5% топ-1/2/3 плюс 65% пропорционально — отменены.
- Внёс деньги, но не сыграл ни одной партии — **не получает ничего**. Членство
  сохраняется.
- Комиссия стола 10% оставлена как была (`[2026-05-17]`); 31.07 не отменялась.
- Казна страны = взносы вступающих воинов **плюс донаты**.

**Публикация в канал при объявлении войны** — `[2026-07-31]`: кто кому объявил,
казна каждой стороны и общая касса, что победитель забирает казну проигравшего
целиком, что деньги делятся кратно числу побед, срок войны и что вступить можно
после начала.

**Вход после начала** — `[2026-07-31]`: вступить в идущую войну можно; система
перетасовывает бойцов и ставит новичка в пары, пока срок не вышел.

Реализация — `services/prizes.ts` (`splitWarPot`), тесты `prizes.test.ts`.
Разбор — `docs/history/DECISIONS.md` § 2026-07-31.

**Донаты в страну:**
- `[2026-04-25 09:54:47]` — **донат при вступлении и при выходе** должен быть.
  Логика конкретных процентов **Кенан не уточнял** (см. §5).

### 3.4 TOURNAMENT
`[2026-07-31]` — **Кенан утвердил распределение**, вопрос закрыт (прежняя запись
«не уточнял, оставляем как есть» больше не действует):

- Касса = взносы **плюс донаты**, включая донаты тех, кто сам не играет.
- **60% / 30% / 10%** между первым, вторым и третьим местом — **независимо от
  типа турнира**. Из каждой доли **10% комиссия стола**, игроку 90% его доли.
  Касса 100 000 → на счёт 54 000 / 27 000 / 9 000, комиссия 10 000.
- **Один участник** забирает всю кассу минус комиссию. Мест меньше трёх — доли
  перенормируются между теми, кто есть.
- Инвариант: выплаты плюс комиссия равны кассе. Остатка, который где-то
  оседает, быть не должно — на этом сгорело 100 000 монет за 10 турниров.
- Вступить можно и в **уже начавшийся** турнир: система назначает новичку
  соревнования до конца чемпионата.

Реализация — `services/prizes.ts` (`splitTournamentPot`), тесты `prizes.test.ts`.
Разбор — `docs/history/DECISIONS.md` § 2026-07-31.

### 3.5 FRIENDLY (по коду)
`[2026-03-30 22:30:50]` упоминает только Battles/War/Tournament/Bot/Lesson/Puzzle.
Friendly mode — следствие реализации `Session.isPrivate=true` без ставок. Кенан
явно его не описывал; оставляем существующее поведение.

### 3.6 PUZZLE
См. §2.14. Источник — Lichess (puzzle DB), реализованы.

---

## 4. Системы

### 4.1 TonConnect
- `[2026-03-30 22:30:50]` — **1 TON единоразово за подключение кошелька** (см. §2.7).
  В коде не реализовано. Открытый вопрос: где списывать (в коде есть `WALLET_UNLOCK`
  в `enum TransactionType`, но логика отсутствует).
- `[2026-04-24 19:55:40]` — bug: на десктопе подтверждает подключение, но
  не возвращает к игре; повторное подключение — ошибка жёлтым текстом. Не починено
  на 25 апреля.

### 4.2 Withdraw (вывод TON)
- В коде заблокирован в v6. Кенан явно не уточнял возобновление; в `[2026-04-25 10:13:46]`
  упоминается «биржа, блокчейн» как часть финального проекта — значит вывод нужен.
  Точный % комиссии Кенан не указал. → Открытый вопрос.

### 4.3 P2P Exchange
- 0.5% комиссия (`[2026-03-30 22:30:50]`) — реализовано.
- Открытый вопрос: «1 TON за подключение кошелька» — см. 4.1.

### 4.4 Referrals
`[2026-03-30 22:30:50]` (привязка к Войнам) +
текущая реализация в коде (см. `GAME_DESIGN_KNOWLEDGE.md` §7):
- 2 уровня, без рекурсии.
- L1 % зависит от **звания** реферера (1..15%) — таблица в `militaryRank.ts`.
- L2 — фиксированные 10%.
- Activation bonus после 1-й завершённой партии нового игрока.
- Реферальная система **= военная система рангов** (см. 4.7).

`[2026-04-03 11:27:51]` (доп. социальные):
- За 3, 5, 10, 20, 50, 100 рефералов — **дополнительные** разовые бонусы 3k/5k/10k/20k/50k/100k.

### 4.5 Tasks (3 этажа)
См. §2.9 + §2.14 + §2.15.
- Этаж 1 (ежедневные): 5/день, 1k..20k за задачу, рандом из Lichess.
- Этаж 2 (обучение): последовательно открывается, 1k + 1k×level.
- Этаж 3 (социальные): из админки/бота — подписки, рефералы.

### 4.6 ELO / League
- ELO по классике (`[2026-04-06 08:52:32]`), K=32 — в коде.
- Лиги (BRONZE..STAR) — по балансу, не по ELO. Кенан явно лиги по ELO не оспаривал;
  оставляем код.

### 4.7 Military Ranks (звания)
В коде 18 рангов. `[2026-03-30 22:30:50]` — звание = «по реферальной системе»;
главкомом становится тот, у кого выше звание. **CANONICAL:** это правда, не оспорено.

### 4.8 JARVIS levels (20)
См. §3.2. Награды в `config.ts:84-105` (1k..1M) — это правда; устаревшие 1k..75k в
JarvisModal.tsx — нужно обновить.

### 4.9 Attempts (попытки)
- `[2026-04-04 11:26:52]` — «таймер должен давать +1 попытку каждые 8 часов» —
  совпадает с кодом (00:00 / 08:00 / 16:00 UTC, +1 каждые 8ч).
- Покупка попыток — модал, см. §2.1 п.4.

### 4.10 Coin Economy / Donations
- Эмиссия: hard cap, фазы — в коде. Кенан явно цифры не пересматривал.
- Комиссия батлов: **10%** (`[2026-04-21 22:00:42]`) — совпадает с кодом
  (`BATTLE_COMMISSION_PERCENT=10`).
- Зрительский донат на партию — обязателен в публичных батлах
  (`[2026-04-21 22:00:42]`); реализовано в `finish.ts` (`donationPool`).
- Донат в кассу страны — обязателен (`[2026-04-25 09:54:47]`); реализовано в `wars.ts:732`.

### 4.11 Notifications (Telegram-пуши)
- `[2026-04-19 02:18:13]` — toast 3 сек на главной при «нельзя играть».
- `[2026-04-04 11:12:31]` — уведомления, которые показываются один раз игроку при
  первом входе на страницу — больше не показываются. Состояние «прочитано» сохраняется.
- Бот пушит: победа партии, объявление войны, старт войны, призыв в турнир, активация
  реферала. Кенан явно подтверждал важность пушей `[2026-04-19 02:48:54]`.

### 4.12 Splash / Auth
См. 1.8 + `[2026-04-25 09:54:47]` — на 25 апреля **сломано на мобильном** (жёлтый
экран `tg.ready()`/initData). Должно: иконка лошади ×2 без синего фона; корректная
авторизация через Telegram initData + JWT (как в `useAuth.ts`).

### 4.13 Sounds
Кенан явно не уточнял конкретные звуки.

### 4.14 Themes / Skins
- `[2026-04-03 17:16:43]` — «игровую доску сделать красивее, тема, по стереометрии,
  доска и игровая площадка обе должны быть в шаблоне».
- Premium Oak — дефолт (из MEMORY).
- Скины покупаются в магазине, применяются мгновенно (см. §2.6).

### 4.15 Avatars
- Загрузка фото пользователя.
- В каждой карточке батла, в полосе игрока на доске — кликабельный аватар, переход на
  профиль (`[2026-04-05 19:25:04]` — критический bug-fix).
- Размер аватара в карточке — `[2026-04-04 13:32:45]` — увеличить ≈+50%, слово «топ»
  убрать после имени.

### 4.16 Modals
- `[2026-04-25 09:54:47]` — модал выхода из страны должен быть **по центру** (не внизу).
  → правило: критические модалы (подтверждения, выход из режима, покупка) — по центру.
  Bottom-sheet — только для контекстных «лёгких» вызовов.
- Модал создания партии (Battle/Jarvis) — единый шаблон (`[2026-04-04 13:16:36]`).
- Модал покупки попыток / монет — overlay поверх текущей страницы; после покупки —
  возврат на ту же страницу (`[2026-04-04 13:52:26]`).

### 4.17 Confirmations
- `[2026-04-25 09:54:47]` — `confirm` перед выходом из активной партии — обязателен.

### 4.18 Toasts / Empty states
- Уведомления — toast длительностью 3 сек (см. 4.11).
- Empty states (например, «нет активных сессий») — соответствующая панель не
  показывается, не пустой блок (`[2026-04-03 10:16:36]`).

---

## 5. Несостыковки и открытые вопросы (нужно решение Кенана)

| # | Тема | Статус |
|---|---|---|
| 1 | **N дней бездействия** для авто-смены главкома | **РЕШЕНО** `[2026-04-25]` — **7 дней** неактивности → авто-переход титула к топу по победам (`warWins desc`). |
| 2 | **% доната при вступлении/выходе из страны** | **РЕШЕНО** `[2026-04-25]` — взнос как в батлах (мин **10 000 ᚙ**, до баланса). При выходе игрок забирает свой первичный вклад. При победе казна противника делится **пропорционально вкладам** + **бонус лидерам**. Лидер по сумме взносов → главком. |
| 3 | **Approve/Reject новых членов страны командиром** | **РЕШЕНО** `[2026-04-25]` — модерация **обязательна** (approve/reject от главкома). В коде убрать open-join. |
| 4 | **1 TON за подключение кошелька** | **РЕШЕНО** `[2026-04-25]` — реализовать **1 TON unlock** (одноразово, навсегда). |
| 5 | **% комиссии вывода TON** | **РЕШЕНО** `[2026-04-25]` — **0.5%** со всех TON-операций (биржа + вывод) + 1 TON unlock. |
| 6 | **Структура bracket турниров** (single-elim/swiss) | **РЕШЕНО** `[2026-04-25]` — **Swiss-system**, размеры **4 / 8 / 16 / 32** игрока. |
| 7 | **Награды стандартных тасков** (DAILY_LOGIN, FIRST_GAME, WIN_STREAK) | Не зашиты в коде, не уточнены Кенаном. Заполнить в seed. |
| 8 | **Расхождение reward в JarvisModal.tsx vs config.ts** | Кенан говорил «увеличиваем награду по уровню»; верно `config.ts` (1k..1M). Удалить устаревшие числа из JarvisModal.tsx. |
| 9 | **Античит / детекция Stockfish-помощи** | **РЕШЕНО (для MVP)** `[2026-04-25]` — серверная валидация ходов + контроль времени уже есть; engine-detection **отложено** на пост-MVP. |
| 10 | **Login streak / Чемпион месяца** — cron-логика | Поля есть, логика начисления неполна. Кенан не уточнял правила. |
| 11 | **Friendly mode (по коду без ставок)** | Кенан не описывал. Оставить как есть. |
| 12 | **Иконка приложения / splash на мобильном** | `[2026-04-25 09:54:47]` — старая синяя стоит. Bug, не «открытый вопрос», но **не закрыт**. |
| 13 | **Активные сессии — модал** | `[2026-04-03 11:27:51]` указал дизайн. Не реализован к 25 апреля. |
| 14 | **«Вкладыши Вызов/Сражение/История»** в BattlesPage | `[2026-04-25 09:54:47]` — Кенан кричит «зачем убрал?» — он хотел оставить именно эти категории как **этапы карточки**, а не как табы. Нужно подтверждение: вернуть их как этапы или как табы? Канон прочитан как «этапы» (см. §2.2). |
| 15 | **«Стопка монет» иконка** vs CoinIcon | Канон: только CoinIcon. На 25 апреля где-то ещё есть старые иконки. |

---

## 6. Цитатник ключевых реплик

- `[2026-03-30 22:30:50]` Кенан: **полный канон режимов** (см. §3, §4 — основной
  источник для пунктов «Битвы / Войны / Турниры / Магазин / TON / Биржа /
  Многоязычность»).
- `[2026-03-22 16:03:57]` Кенан: «не более 10 партий одновременно за войну» — основа §3.3.
- `[2026-04-03 10:34:41]` Кенан: пять правил отображения паспортной карточки — §2.1.
- `[2026-04-03 11:27:51]` Кенан: HomePage navigation map — §2.1, §4.5.
- `[2026-04-21 22:00:42]` Кенан: «батлы публичные/приватные — разница» — §2.2.
- `[2026-04-25 09:54:47]` Кенан: финальный список регрессий — §5 (12, 13, 14, 15).
- `[2026-04-25 10:13:46]` Кенан: **запрос на этот документ**.

---

## 7. Сверка с GAME_DESIGN_KNOWLEDGE.md (что есть в коде vs. требуется)

| Элемент | Канон | Код (v7.1.0) | Дельта |
|---|---|---|---|
| Battle commission 10% | требуется | `BATTLE_COMMISSION_PERCENT=10` | OK |
| 0.5% TON exchange fee | требуется | `PLATFORM_FEE_PERCENT=0.005` | OK |
| 1 TON wallet unlock fee | требуется | **НЕТ** | реализовать |
| 10 одновременных war-партий | требуется | `wars.ts:641` ровно так | OK |
| Approve главкома при join | требуется | open join | реализовать |
| Авто-смена главкома по бездействию (7д) | требуется | сортировка по `warWins` | реализовать |
| Donation на vstup/leave страны | требуется | только треасуру через `donate` | реализовать |
| BattlesPage: вкладыши «этапы карточки» | требуется | вкладки убраны | вернуть как состояния карточки |
| HomePage модал «Активные сессии» (3 доски) | требуется | НЕТ | реализовать |
| Модал выхода из страны по центру | требуется | внизу | переверстать |
| CoinIcon везде, без старых стопок | требуется | смешанно | пройти везде |
| Иконка приложения без синего фона | требуется | старая синяя на мобильном | заменить |
| Confirm перед выходом из партии | требуется | сломано/откатили | восстановить |
| Награды Jarvis в фронте 1k..1M | требуется | в `JarvisModal.tsx` устаревшие | синхронизировать |
| Социальные таски с бонусами 3k..100k | требуется | НЕТ | реализовать |
| Lesson этаж: уровни open-by-progress | требуется | частично | дореализовать |
| Daily 5 в день из Lichess | требуется | реализовано | OK |
| Кликабельный аватар → профиль игрока | требуется | частично починено | докатить |
| TG-уведомления как toast 3 сек | требуется | смешанные тосты | стандартизовать |

---

## 8. Сводка

- Реплик Кенана всего: **2 365**.
- Реплик с привязкой к 47 элементам: **2 826** (одна реплика часто бьёт несколько слотов).
- После фильтра «session continued»-шума и классификации `req|bug|neg`: **426** прямых
  требований/багов.
- Канонических утверждений в этой спеке: **~140** (по 47 элементам).
- Элементов с противоречиями (история нужна): **9**
  (BattlesPage-вкладыши, JarvisModal как часть Home, иконка монеты, donation страны,
  approve главкома, N дней главкома, активные сессии модал, модал выхода центр/низ,
  таблица призов турниров).
- Нерешённых открытых вопросов: **9** (см. §5; Q1–Q6 закрыты `[2026-04-25]`, осталось 7, 8, 10–15).
