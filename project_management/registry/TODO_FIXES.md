# 🧾 ОТКРЫТЫЕ ДЕФЕКТЫ CHESSCOIN

> Заведён 29.07.2026. Сюда попадает всё, что найдено, но не исправлено —
> включая то, что нашла по дороге и **не стала чинить, потому что вне задачи**.
> Закрытие пункта = ✅ + запись в `chesscoin_pm.regression_cases` (иначе не закрыт).
>
> Обозначения: 🔴 ломает продукт · 🟠 мешает работе · 🟡 долг/риск

## Закрыто 29.07.2026

| № | Что было | Чем закрыто | Доказательство |
|---|---|---|---|
| 1 | 🔴 Бот 19 дней ретраил уведомление заблокировавшему пользователю каждые 30 сек (с 10.07 17:34, ~2866 ошибок в сутки, ~54 000 всего) | `PERMANENT_ERRORS` в `bot/handlers/notifications.py`: `TelegramForbiddenError`/`TelegramBadRequest` снимают уведомление с очереди | на проде одно снятие в 17:16:17, дальше 0 строк `Failed to send notification`. Эталон: «Бот не зациклен» |
| 2 | 🟠 Бэкапа проекта не существовало: код, аватары, память, `.env` нигде не сохранялись по расписанию | `scripts/backup_chesscoin.sh` — код с историей (git bundle) + БД + память контура + аватары + секреты (AES, ключ вне архива) → GDrive `chesscoin-backups/` + Telegram; таймер `chesscoin-backup.timer` 04:30 | архив распакован и восстановлен: клон из бандла (370 файлов), `env.enc` расшифрован, дамп 40 таблиц. Эталон: 2 случая |
| 3 | 🟠 `gh` CLI и push: `HTTP 401 Bad credentials` | **Диагноз был неверный.** Токен не «протух» — протухшая переменная `GH_TOKEN` перекрывала рабочие доступы Windows Credential Manager | `unset GH_TOKEN GITHUB_TOKEN` → push прошёл. Записано в `agent_mistakes` как моя ошибка |
| 4 | 🟡 Рабочая копия отстала (−21 и −86 коммитов), три `prunable` worktree на исчезнувший Desktop-путь | `git worktree prune`, ветка влита в `main` | локально = GitHub = прод на `7d7a366`. Эталон: «Прод не отстаёт от origin/main» |
| 9 | 🔴 Канон дизайна не версионировался: `.gitignore` строка 4 (`.claude/`) — `git ls-files \| grep TPL` = **0** | 9 файлов перенесены в `design_canon/`, ссылки в `CLAUDE.md` обновлены | `git ls-files design_canon` = 10, на проде тоже. Эталон: «Канон дизайна версионируется в git» |
| 10 | 🟡 `README.md` отправлял читать 4 несуществующих файла (`.claude/STARTUP.md`, `.claude/INIT.md`, `.claude/management/*`) | README переписан на реальный управляющий контур | ссылки ведут в `project_management/` |
| 11 | 🔴 **Канон требований Кенана не версионировался**: вся папка `чат/` под `.gitignore` (строка 71), а `CLAUDE.md` требовал её читать | 8 спецификаций вынесены в `docs/kenan_canon/` (208 КБ); сырые дампы (21 МБ) остаются вне git — они в БД и бэкапах | `git ls-files docs/kenan_canon` = 8, на проде тоже |
| 12 | 🟡 Беспорядок в корне: 41 отслеживаемый файл вперемешку, чужие скрипты 994/Claudia | `docs/{kenan_canon,product,history,design,safety}`, `archive/{legacy_tools,snapshots}`; ничего не удалено | в корне 9 файлов; `docs/STRUCTURE.md` — карта |

## Закрыто 30.07.2026

| № | Что было | Чем закрыто | Доказательство |
|---|---|---|---|
| 5 | 🟡 Мёртвые IP в рабочих документах | `AGENTS.md` — адреса исправлены на `45.67.216.36`. В исторические документы (`SAFETY_POLICY`, `MASTERPLAN`, `PROJECT_VISION`) добавлена шапка-предупреждение: адреса устарели, источник истины — `CLAUDE.md` § СЕРВЕРА | `grep 185.203 AGENTS.md` → пусто |
| 6 | 🟡 Фикс pgbouncer не reset-safe | **Оказался уже reset-safe**: `docker-compose` патчит `pg_hba.conf` при каждом старте (`sed scram→md5`) и поднимает postgres с `password_encryption=md5`; `POSTGRES_INITDB_ARGS` закрывает новые тома. Запись в `MASTER_PLAN` § 5.7 устарела | хэши обеих ролей — `md5`; `pg_hba` весь `md5`. Эталон: «Пароли ролей в md5» |
| 7 | 🟡 Остатки на проде: `docx_unzipped/`, `чат/` | перенесены в `/opt/chesscoin-archives/2026-07-30_leftovers/` с `README.txt`; ничего не удалено | в корне прода было 22 записи, стало 20; архив 2.7 МБ |
| 8 | 🟡 Нет публичного health-эндпоинта | **Мой ошибочный диагноз.** Он есть — `https://chesscoin.app/health` → `200 {"status":"ok","db":"ok"}`. Проверяла `/api/health` (там 404 by design) и объявила отсутствие | `curl`. Эталон: «Публичный /health отвечает снаружи». Ловушка внесена в `prod_path_registry`, повтор корня — в `agent_mistakes` |
| 13 | 🟡 Доставка бэкапов зависела от бота Claudia | цепочка каналов: свой бот → группа → **личка владельца** → архивариус. Проект больше не зависит от чужого бота | прогон `yellow-fixes`: «канал: свой бот → личка владельца», `sendDocument ok:true` |
| 15 | 🟡 `/battles` не снимался в эталон | причина — короткое ожидание (2 с), экран рисует список + слайдер подсказки. Ожидания подняты до 3.5–6 с | эталон расширен до **18 экранов**, `compare` 18/18 |

## Открыто

| № | Уровень | Что | Найдено | Доказательство |
|---|---|---|---|---|
| 19 | 🔴 | ~~`POST /profile/ton/buy` начислял монеты по числу из тела запроса без проверки блокчейна (1 TON = 1 000 000 монет). На проде 3 начисления на 11 442 500 монет~~ | 30.07 | **✅ закрыт 30.07** — сумма берётся из транзакции toncenter, `ton_deposits.txHash` в первичном ключе не даёт предъявить платёж дважды, без `PLATFORM_TON_WALLET` → 503. Проверено на проде: запрос на 1000 TON → **HTTP 402**, баланс не изменился. В эталоне |
| 20 | 🔴 | **Пост в канал не работал ни разу**: `TELEGRAM_CHANNEL_ID` отсутствует в `.env` прода (`sendToChannel` молча выходит) + порог `bet > 10000` отсекает стандартную ставку ровно 10 000 — под условие попадают 3 батла из 43 | 30.07 | `grep CHANNEL /opt/chesscoin/.env` → 0; распределение ставок. **Нужен id канала от Кенана** |
| 21 | 🟠 | **Реальные батлы не доигрываются**: 6 из 50 за всё время, 0 из 7 за последние 30 дней. 41 отменён (22 не стартовали, 19 «стартовали» без единого хода). Против Джарвиса доигрывается 82% | 30.07 | статистика `sessions`; деньги при этом сходятся (списано 1 939 000, вернулось 1 907 000) |
| 22 | 🟠 | **Биржа не провела ни одной сделки**: 10 ордеров `OPEN` с 3–5 апреля, 6 p2p-ордеров отменены в мае, исполненных — 0. Эндпоинты `execute`/`fill` существуют | 30.07 | выборка `trade_orders`, `p2p_orders` |
| 23 | 🟡 | Возврат ставки пишется **тремя разными типами** транзакций: `BATTLE_BET` со знаком плюс, `BATTLE_WIN` с флагом refund, `REFUND`. Экономическую отчётность по типам построить нельзя | 30.07 | `socket.ts:795`, `bot.ts:513`, `cleanup.ts:97` |

| 17 | 🟠 | **Остаток дрейфа схемы** — сознательно не применяется: `DROP TABLE` шести таблиц (среди них чужие `claudia_*`, `conversations`, `task_buffer`), `DROP COLUMN` с живыми данными (`users.countryCode` — 7 значений), перевод `trade_orders` в enum. Разбор по пунктам — [SCHEMA_DRIFT.md](SCHEMA_DRIFT.md) | 29.07 | `migrate diff` = 100 строк; в эталоне страж «Дрейф схемы не растёт» |
| 18 | 🟡 | Бот `@chessgamecoin_bot` не добавлен в группу бэкапов. Доставка работает (личка владельца), но групповой канал включится только после ручного шага Кенана | 30.07 | `getChat` для группы: `chat not found` |

## Продуктовый бэклог требований Кенана

Не дублируется здесь: живёт в `MASTER_PLAN.md` § 2 (регрессии A1–A6, новые
системы B1–B7, стандартизация C1–C4), `docs/product/MECHANICS.md` и
`docs/kenan_canon/`. Этот файл — только про **дефекты**, а не про нереализованные фичи.
