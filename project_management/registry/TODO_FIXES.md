# 🧾 ОТКРЫТЫЕ ДЕФЕКТЫ CHESSCOIN

> Заведён 29.07.2026. Сюда попадает всё, что найдено, но не исправлено —
> включая то, что нашла по дороге и **не стала чинить, потому что вне задачи**.
> Закрытие пункта = ✅ + запись в `chesscoin_pm.regression_cases` (иначе не закрыт).
>
> Обозначения: 🔴 ломает продукт · 🟠 мешает работе · 🟡 долг/риск

## Закрыто 29.07.2026

| № | Что было | Чем закрыто | Доказательство |
|---|---|---|---|
| 1 | 🔴 Бот 19 дней ретраил уведомление заблокировавшему пользователю каждые 30 сек (с 10.07 17:34, ~2866 ошибок в сутки, ~54 000 всего) | `PERMANENT_ERRORS` в `bot/handlers/notifications.py`: `TelegramForbiddenError`/`TelegramBadRequest` снимают уведомление с очереди | на проде одно снятие в 17:16:17, дальше 0 строк `Failed to send notification`. Эталон: «Бот не зациклен» |
| 2 | 🟠 Бэкапа проекта не существовало: код, аватары, память, `.env` нигде не сохранялись по расписанию | `scripts/backup_chesscoin.sh` — код с историей (git bundle) + БД + память контура + аватары + секреты (AES, ключ вне архива) → GDrive `chesscoin-backups/` + Telegram; таймер `chesscoin-backup.timer` 04:30 | архив распакован и восстановлен: клон из бандла (370 файлов), `env.enc` расшифрован, дамп 40 таблиц. Эталон: 2 случая |
| 3 | 🟠 `gh` CLI и push: `HTTP 401 Bad credentials` | **Диагноз был неверный.** Токен не «протух» — протухшая переменная `GH_TOKEN` перекрывала рабочие доступы Windows Credential Manager | `unset GH_TOKEN GITHUB_TOKEN` → push прошёл. Записано в `agent_mistakes` как моя ошибка |
| 4 | 🟡 Рабочая копия отстала (−21 и −86 коммитов), три `prunable` worktree на исчезнувший Desktop-путь | `git worktree prune`, ветка влита в `main` | локально = GitHub = прод на `7d7a366`. Эталон: «Прод не отстаёт от origin/main» |
| 9 | 🔴 Канон дизайна не версионировался: `.gitignore` строка 4 (`.claude/`) — `git ls-files \| grep TPL` = **0** | 9 файлов перенесены в `design_canon/`, ссылки в `CLAUDE.md` обновлены | `git ls-files design_canon` = 10, на проде тоже. Эталон: «Канон дизайна версионируется в git» |
| 10 | 🟡 `README.md` отправлял читать 4 несуществующих файла (`.claude/STARTUP.md`, `.claude/INIT.md`, `.claude/management/*`) | README переписан на реальный управляющий контур | ссылки ведут в `project_management/` |
| 11 | 🔴 **Канон требований Кенана не версионировался**: вся папка `чат/` под `.gitignore` (строка 71), а `CLAUDE.md` требовал её читать | 8 спецификаций вынесены в `docs/kenan_canon/` (208 КБ); сырые дампы (21 МБ) остаются вне git — они в БД и бэкапах | `git ls-files docs/kenan_canon` = 8, на проде тоже |
| 12 | 🟡 Беспорядок в корне: 41 отслеживаемый файл вперемешку, чужие скрипты 994/Claudia | `docs/{kenan_canon,product,history,design,safety}`, `archive/{legacy_tools,snapshots}`; ничего не удалено | в корне 9 файлов; `docs/STRUCTURE.md` — карта |

## Закрыто 30.07.2026

| № | Что было | Чем закрыто | Доказательство |
|---|---|---|---|
| 5 | 🟡 Мёртвые IP в рабочих документах | `AGENTS.md` исправлен; в исторические документы добавлена шапка «адреса устарели, источник истины — `CLAUDE.md` § СЕРВЕРА» | `grep 185.203 AGENTS.md` → пусто |
| 6 | 🟡 Фикс pgbouncer не reset-safe | **Оказался уже reset-safe**: compose патчит `pg_hba.conf` при каждом старте + `password_encryption=md5` | хэши обеих ролей `md5`. Эталон: «Пароли ролей в md5» |
| 7 | 🟡 Остатки на проде `docx_unzipped/`, `чат/` | перенесены в `/opt/chesscoin-archives/2026-07-30_leftovers/`, ничего не удалено | в корне прода 20 записей вместо 22 |
| 8 | 🟡 Нет публичного health | **Мой ошибочный диагноз**: он есть на `/health`, проверяла `/api/health` | `curl` → `200 {"status":"ok","db":"ok"}`. Эталон + запись в `agent_mistakes` |
| 13 | 🟡 Доставка бэкапов зависела от бота Claudia | цепочка: свой бот → группа → личка владельца → архивариус | прогон: «свой бот → личка владельца», `sendDocument ok:true` |
| 15 | 🟡 `/battles` не снимался в эталон | короткое ожидание (2 с) — экран рисует список и слайдер подсказки | эталон 18 экранов, `compare` 18/18 |
| 18 | 🟡 Бот не в группе бэкапов | Кенан добавил `@chessgamecoin_bot` в группу | `getChatMember` → `status: administrator` |
| 19 | 🔴 **TON-пополнение печатало монеты**: начисление по числу из запроса, 1 TON = 1 000 000 монет. На проде 3 начисления на 11 442 500 монет | сумма берётся из транзакции toncenter; `ton_deposits.txHash` в первичном ключе не даёт предъявить платёж дважды; нет `PLATFORM_TON_WALLET` → 503 | на проде запрос на 1000 TON → **HTTP 402**, баланс не изменился. Эталон |
| 20 | 🔴 **Пост в канал не работал ни разу** | `TELEGRAM_CHANNEL_ID` в `.env` **и проброс в compose** (без него переменная не доходила до контейнера); порог `>10000` → `>= 10000`; WARNING вместо тихого выхода | живой пост в канал: `sendMessage ok:true` (msg 503), `deleteMessage ok:true`. Заодно задание на подписку вело на несуществующий `@chesscoin_official` |
| 23 | 🟡 Возврат ставки писался тремя типами | везде `REFUND`: `socket.ts` (был `BATTLE_BET` плюсом), `bot.ts` (был `BATTLE_WIN` с флагом refund), `finish.ts` ничья | 147 тестов зелёные. Эталон: «Возврат пишется типом REFUND» |
| 21а | 🟠 **Зависшие вызовы держали ставку неделями** (чистка через 7–30 дней или кнопкой админа) | `cleanupStaleBattles`: порог 1 час, запуск каждые 15 минут, возврат типом `REFUND` | на проде: `[Cron/StaleBattles] Снято зависших вызовов: 2, ставки возвращены` — заявки от 24.07 сняты, ожидающих 0. Эталон: «Нет вызовов старше 2 часов» |
| 22 | 🟠 **Биржа не провела ни одной сделки** и содержала три дефекта | (1) любой выдуманный `txHash` давал бесплатные монеты — `PENDING` трактовался как «разрешаем»; (2) частичная покупка удваивала остаток; (3) продажа писалась в `transactions` отрицательной суммой мимо `updateBalance`, ломая инвариант | на проде: `FAKE_HASH` → **HTTP 402**, ордер остался `OPEN` 50 000, баланс покупателя не изменился; отмена вернула заморозку; расхождений баланса 0. Старые ордера (10+6) в архив и удалены |
| 24 | 🔴 Сеть TON: `testnet` при mainnet-кошельке, пустой ключ | `TON_NETWORK=mainnet`, ключи mainnet и testnet прописаны | изнутри контейнера: `toncenter ok: true`, 3 транзакции кошелька. Страж согласованности |
| 25 | 🟡 Иконка манифеста 404 | взят готовый премиум-аватар «золотой конь», 192×192 PNG | `200 image/png`, страж в эталоне |


## Открыто

| № | Уровень | Что | Найдено | Доказательство |
|---|---|---|---|---|
| 28 | 🔴 | **Сорванное завершение партии не самовосстанавливается.** Если finish упал (например, дедлок), партия остаётся в матовой позиции как `IN_PROGRESS`, а позже закрывается уборкой как отмена — матч засчитывается **ничьёй 0.5/0.5 вместо победы**. Проверено на живой турнирной партии 30.07: мат `Qh4#` в PGN, победитель «НЕ ОПРЕДЕЛЁН» | 30.07 | `tournament_matches`: winnerId пуст, очки 0.5/0.5 при мате на доске |
| 29 | 🟡 | **`window.innerWidth` в `LessonPage.tsx:298`** — прямое нарушение канона (правило 14: только CSS media queries) | 30.07 | `grep window.innerWidth frontend/src/pages/LessonPage.tsx` → 1 |
| 17 | 🟠 | **Остаток дрейфа схемы** — сознательно не применяется: `DROP TABLE` шести чужих таблиц (`claudia_*`, `conversations`, `task_buffer`), `DROP COLUMN` с живыми данными, перевод `trade_orders` в enum. Разбор — [SCHEMA_DRIFT.md](SCHEMA_DRIFT.md) | 29.07 | страж считает дрейф без архивных таблиц, порог 104 |
| 21б | 🟠 | **Батлы: вызов некому принять.** Механика закрыта, объявление в канале опубликовано, анонс вызова уходит мгновенно. Осталось дождаться живых игроков | 30.07 | 88 юзеров, активных единицы |
| 27 | 🟡 | **Фронт шлёт псевдо-хэш вместо настоящего.** `tonconnect.ts` делает `txHash` из base64 BOC — бэкенд его больше не использует (ищет платёж по факту), но поле остаётся вводящим в заблуждение. Стоит извлекать реальный хэш через `@ton/core` либо не слать вовсе | 30.07 | `frontend/src/lib/tonconnect.ts:168` |

## Продуктовый бэклог требований Кенана

Не дублируется здесь: живёт в `MASTER_PLAN.md` § 2 (регрессии A1–A6, новые
системы B1–B7, стандартизация C1–C4), `docs/product/MECHANICS.md` и
`docs/kenan_canon/`. Этот файл — только про **дефекты**, а не про нереализованные фичи.
