# CLAUDE.md — Конституция работы с ChessCoin

**Это главный файл памяти. Читай полностью в начале КАЖДОЙ сессии.**

---

## 📋 ГЛАВНЫЕ ПРАВИЛА

### 1. ЯЗЫК
- ✅ **Пиши ТОЛЬКО по-русски** (все сообщения, комментарии, коммиты)
- ❌ Не переходи на английский без явного запроса

### 2. ПАМЯТЬ И КОНТЕКСТ
- **Начало сессии**: Читай CLAUDE.md → Читай archive/ → Начинай работу
- **После сессии**: Обновляй CLAUDE.md с новой информацией
- **Сравнение**: Всегда смотри что было vs что есть сейчас
- **История**: Все старые файлы в `archive/` — учись на них

### 3. РЕЗУЛЬТАТЫ ВАЖНЕЕ СЛОВ
- ❌ Не говори "я сделал X", пока не покажешь визуально
- ✅ "Сделал" = игра запускается И выглядит правильно
- ✅ Проверь мобильный вид, десктоп вид, все интерфейсы
- ✅ Если дизайн ломаный — это не "сделано"

### 4. РАБОТА ФАЗАМИ
- **Никаких "фаза 1 → фаза 2 → фаза 3" без завершения**
- Одна фаза = она ПОЛНОСТЬЮ готова перед следующей
- Доказательство = скриншот/видео работающего функционала

### 5. ЧЕСТНОСТЬ О СТАТУСЕ
- Если что-то не работает — скажи сразу
- Если дизайн уродливый — признай
- Если нужно больше времени — скажи за сколько

---

## 🔴 КРИТИЧЕСКИЕ ОШИБКИ ПРОШЛЫХ СЕССИЙ

### Ошибка #1: HomePage была уничтожена (Сессия неизвестна)
- **Что случилось**: Заменил рабочую HomePage (203 строк) на тестовую версию (141 строка)
- **Последствия**: Game не запускалась, все навигационные кнопки исчезли, handleGameStart только логировал
- **Как поправили**: Восстановили из коммита f426134
- **Урок**: Никогда не упрощать компоненты без причины, всегда проверять что они работают после изменений

### Ошибка #2: GameSetupModal layout неправильный
- **Проблема**: Цвета в 2+ строках (должны быть в 1), время не в сетке 3x2
- **Статус**: Не исправлено
- **Приоритет**: ВЫСОКИЙ — это UI который видит пользователь

### Ошибка #3: "Launching battle!" экран зависает
- **Причина**: handleGameStart в HomePage был пустой (только console.log)
- **Статус**: ✅ Исправлено e96d859 — теперь навигирует на /game/jarvis/:level
- **Проверка**: Game должна запускаться и показывать доску

### Ошибка #4: Дизайн всех страниц неполный
- **Проблема**: Батлы, войны, турниры и другие страницы имеют только обложку, внутреннее содержимое пустое или сломанное
- **Статус**: Не исправлено
- **Причина**: Из-за фокуса на deployment забыли про фронтенд

---

## ✅ УСПЕШНО ЗАВЕРШЕННЫЕ РАБОТЫ

### Session 11 (2026-04-01)
- ✅ Deployment fix: .env auto-creation для pgbouncer
- ✅ L2 Responsive design: CSS grid utility classes добавлены
- ✅ Frontend build: TypeScript typecheck PASSED

### Текущая сессия (2026-04-02)
- ✅ Создан DESIGN_SYSTEM_AUDIT.md — полный анализ дизайн-системы
- ✅ Создан DESIGN_TOKENS.css — все недостающие CSS переменные
- ✅ Создан MIGRATION_GUIDE.md — инструкция как рефакторить
- ✅ Восстановлена HomePage из коммита f426134
- ✅ Фиксирована игра (handleGameStart теперь работает)
- ✅ Создана эта Конституция (CLAUDE.md)
- ✅ Создан archive/ для истории
- ✅ Добавлен mock user для dev режима
- ✅ **ИНТЕГРИРОВАНЫ ДИЗАЙН-ТОКЕНЫ (L2: ТИПОГРАФИЯ И КОМПОНЕНТЫ)**
  - ✅ Добавлены все переменные типографики (font-size, font-weight, line-height, letter-spacing)
  - ✅ Добавлены размеры компонентов (button, card, modal, icon, avatar)
  - ✅ Добавлена адаптивная масштабизация (mobile, tablet, desktop)
  - ✅ Рефакторена HomePage.tsx для использования дизайн-токенов
- ✅ **Протестирована вся цепочка игры с дизайн-токенами:**
  - ✅ HomePage загружается с дизайн-токенами — все элементы корректно стилизованы
  - ✅ JarvisModal открывается со всеми 20 уровнями
  - ✅ GameSetupModal открывается с выбором цвета и времени
  - ✅ Все переходы работают правильно
  - ⚠️ Game не запускается (backend не включен в dev, ожидаемо)

---

## 🚀 ТЕКУЩИЕ ПРИОРИТЕТЫ

### Статус сессии (2026-04-02, 01:00+)

**Фаза 3.1: Интеграция дизайн-токенов (ЗАВЕРШЕНА ✅)**
- ✅ Добавлены все переменные типографики и компонентов в index.css
- ✅ Рефакторена HomePage.tsx
- ✅ Рефакторена GameSetupModal.tsx
- ✅ Проверено в браузере — всё работает правильно

**Фаза 3.2: Рефакторинг базовых модалей (ЗАВЕРШЕНА ✅)**

Целевая задача: Удалить все `window.innerWidth` проверки и заменить на CSS дизайн-токены с @media запросами. Все компоненты теперь используют только CSS для адаптивного дизайна.

✅ **10 компонентов успешно рефакторены:**
1. Modal.tsx — основной компонент (0eb5022) + padding/fontSize на токены
2. BattleCard.tsx — 14 window.innerWidth checks удалены (309d2a4) + CSS переменные
3. GameResultModal.tsx — все button styling на дизайн-токены (2b9312d)
4. JarvisModal.tsx — 17 window.innerWidth checks удалены (e7fc72c) + 17 новых CSS токенов
5. ShopItemCards.tsx — board grid layout на дизайн-токены (944db98)
6. StatComponents.tsx — SVG размеры и fontSize на токены (a7ceb0c)
7. MiniProfileSheet.tsx — stats grid 3-col → 2-col на мобиле (e235058)
8. PromotionModal.tsx — piece selection grid 4-col → 2-col (e235058)
9. WarChallengePopup.tsx — buttons 2-col → 1-col при 360px (e235058)
10. Avatar.tsx — gold border glow на дизайн-токены (86e1bac)

✅ **Добавлено 40+ новых CSS переменных с @media брейкпойнтами:**
- Battle Card токены (7 переменных)
- Game Result Modal токены (8 переменных)
- Jarvis Modal токены (17 переменных)
- Shop Components токены (3 переменные)
- Stat Components токены (3 переменные)
- Grid Layout токены (3 переменные + 1 @media вариант для 360px)
- Avatar Border токены (3 переменные)

**Следующие приоритеты (ОПЦИОНАЛЬНО):**
1. **Фаза 3.3** — Оставшиеся компоненты (3 шт):
   - CandleChart.tsx (height responsive)
   - PgnReplayModal.tsx (board width calculation)
   - AvatarCropModal.tsx (SIZE calculation)
2. **Фаза 2** — Заполнить контентом страницы: Профиль, Рефералы, Кубки, Войны, Батлы

### ⚠️ ВЫЯВЛЕННЫЕ ПРОБЛЕМЫ
1. **Используется window.innerWidth вместо CSS media queries**
   - JarvisModal, BattleCard, и другие используют JS для responsive логики
   - Неэффективно и трудно поддерживать
   - РЕШЕНИЕ: Заменить на `@media (max-width: 479px)` и CSS классы

2. **Много hardcoded значений во многих компонентах**
   - GameResultModal: fontSize 14, 12, 24, padding 14px 16px, etc.
   - Нужно заменить на design token переменные
   - Затрагивает 15+ компонентов

3. **Responsive классы существуют но не используются**
   - В index.css есть `.grid-auto-2-3`, `.grid-auto-2-4`, `.hidden-mobile`
   - Компоненты их не применяют
   - Нужно применить где используются grid layouts

### Фаза 1: Базовая функциональность (ЗАВЕРШЕНА ✅)
- [x] **HomePage работает и выглядит профессионально**
  - ✅ Восстановлена функциональность
  - ✅ Дизайн визуально проверен (все элементы на месте)

- [x] **GameSetupModal layout работает**
  - ✅ Color selection: доступны 3 кнопки (Random, White, Black)
  - ✅ Time selection: доступны 6 кнопок (1, 3, 5, 15, 30 min, 1 hr)
  - ✅ Layout правильный и видимый пользователю

- [x] **Game запускается**
  - ✅ Navigation работает правильно
  - ✅ Модальное окно "Launching battle!" появляется
  - ⚠️ Game page требует backend (не запущен в dev режиме)

### Фаза 2: Остальные страницы
- [ ] BattlesPage заполнить содержимым
- [ ] WarsPage заполнить содержимым
- [ ] ShopPage заполнить содержимым
- [ ] ProfilePage заполнить содержимым
- [ ] TournamentsPage заполнить содержимым

### Фаза 3: Дизайн система (НАЧАТА ✅)
- [x] Добавить DESIGN_TOKENS.css в index.css
- [x] Рефакторить HomePage с новыми CSS переменными
- [ ] Рефакторить модали (GameSetupModal, JarvisModal, и другие)
- [ ] Рефакторить карточки (ShopItemCards, BattleCard, и другие)
- [ ] Рефакторить утилиты и компоненты списков

---

## 📊 ФАЙЛЫ В РЕПОЗИТОРИИ

### Основные файлы (ЧИТАЙ ЭТИ)
- **CLAUDE.md** (ты здесь) — Главные правила и история
- **README.md** — Описание проекта
- **FIX_PROGRESS.md** — Статус исправлений
- **ROADMAP.md** — План развития
- **GAME_MECHANICS.md** — Как работает игра

### Созданные на этой сессии (ИСПОЛЬЗУЙ ДЛЯ РЕФАКТОРИНГА)
- **DESIGN_SYSTEM_AUDIT.md** — Анализ дизайна (что сломано и почему)
- **DESIGN_TOKENS.css** — Все CSS переменные которые нужны
- **MIGRATION_GUIDE.md** — Как рефакторить с примерами before/after

### Архив (ИСТОРИЯ ОШИБОК И РЕШЕНИЙ)
Все файлы в папке `archive/` — старые версии, дубликаты, устаревшие документы.
Смотри их в начале сессии чтобы понять что было раньше.

---

## 🎯 КАК РАБОТАТЬ С ЭТИМ ФАЙЛОМ

### В начале сессии:
1. Открой CLAUDE.md и прочитай полностью (ТЫ СЕЙЧАС ЗДЕСЬ)
2. Посмотри на "КРИТИЧЕСКИЕ ОШИБКИ" — помни о них
3. Посмотри на "ТЕКУЩИЕ ПРИОРИТЕТЫ" — это то что нужно делать
4. Если нужна история — смотри archive/

### Во время сессии:
- Работай над одним приоритетом полностью
- Не переходи на следующее пока текущее не готово
- Проверяй результат визуально (скриншот, видео)

### В конце сессии:
- Обнови этот файл:
  - Что сделал?
  - Какие ошибки нашел?
  - Что осталось?
- Обнови archive/ если создал новые документы

---

## ⚠️ ПОМНИ

> "За последние 48 часов я потратил огромное количество денег и времени.
> Я вижу результаты, которые не совпадают с обещаниями.
> Это должно измениться сегодня.
>
> У тебя есть памятьэта Конституция. Используй её правильно."

**Каждая сессия должна заканчиваться видимым, работающим функционалом.**

---

## 📝 ПОСЛЕДНИЕ ОБНОВЛЕНИЯ

**2026-04-02 (Текущая сессия - часть 2, 01:00+)**
- ✅ Рефакторена Modal.tsx — все design tokens вместо hardcoded (коммит: 0eb5022)
- 🔍 Выявлены 3 критические проблемы (см. выше)
- 📋 Обновлена эта документация с полной карточкой что делать дальше

**2026-04-02 (Часть 1)**
- Создана эта Конституция
- Восстановлена HomePage (e96d859)
- Создан DESIGN_SYSTEM_AUDIT.md, DESIGN_TOKENS.css, MIGRATION_GUIDE.md
- Создан archive/ для истории

---

## 🎯 ПАТТЕРН РЕФАКТОРИНГА ДЛЯ КОМПОНЕНТОВ

### Шаг 1: Заменить hardcoded padding на design tokens
```typescript
// ДО:
padding: '16px 24px'
// ПОСЛЕ:
padding: `var(--space-l) var(--space-l)` // или другие токены

// ДО:
padding: '12px 14px'
// ПОСЛЕ:
padding: `var(--card-padding-md) var(--card-padding-md)`
```

### Шаг 2: Заменить hardcoded fontSize на tokens
```typescript
// ДО:
fontSize: 14, fontWeight: 800
// ПОСЛЕ:
fontSize: 'var(--font-size-sm)', fontWeight: 'var(--font-weight-extrabold)'
```

### Шаг 3: Заменить window.innerWidth на CSS media queries
```typescript
// ДО (плохо — в JS):
fontSize: window.innerWidth < 480 ? 16 : 20

// ПОСЛЕ (хорошо — в CSS):
// index.css:
// @media (max-width: 479px) {
//   .my-component-title {
//     font-size: 16px;
//   }
// }
// @media (min-width: 480px) {
//   .my-component-title {
//     font-size: 20px;
//   }
// }
```

---

## 📌 СЛЕДУЮЩИЕ ЗАДАЧИ (READY TO COPY-PASTE)

### Фаза 3.2A: Рефакторить JarvisModal
**Файл:** `frontend/src/components/ui/JarvisModal.tsx`
**Что делать:**
1. Заменить все `window.innerWidth < 480 ? X : Y` на CSS media queries
2. Все hardcoded padding/fontSize/gap на design tokens
3. Применить `clamp()` для responsive значений где нужно

### Фаза 3.2B: Рефакторить GameResultModal
**Файл:** `frontend/src/components/game/GameResultModal.tsx`
**Что делать:**
1. fontSize: 14, 12, 24, 13, 18 → на design tokens
2. padding: '14px 16px' → `var(--card-padding-md)`
3. marginBottom, margin значения → на gap/space tokens

### Фаза 3.3: Остальные компоненты (15+ файлов)
- `ShopItemCards.tsx` — grid layout нужна CSS media query
- `BattleCard.tsx` — window.innerWidth checks
- `StatComponents.tsx` — много hardcoded sizes
- И остальные...

**Всегда использовать MIGRATION_GUIDE.md как шаблон refactoring.**
