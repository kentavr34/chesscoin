# CLAUDE.md — ChessCoin

## ⚠️ ПЕРЕД ЛЮБЫМ ДЕЙСТВИЕМ — ЧИТАТЬ MASTER_PLAN.md

**Жёсткое правило**, обязательное для каждой новой сессии Claude:

1. **САМОЕ ПЕРВОЕ** — открыть и прочитать `MASTER_PLAN.md` в корне репо целиком.
2. Прочитать этот файл (`CLAUDE.md`) целиком.
3. Прочитать `чат/GAME_REQUIREMENTS_FROM_KENAN.md` — канон 47 элементов,
   все одобренные Кенаном требования (нельзя ломать).
4. Выполнить чек-лист в `MASTER_PLAN.md` § 7 «В начале сессии»:
   - `getent hosts chesscoin.app` (должно быть `185.203.118.96`)
   - `git log --oneline -10` в worktree
   - `ssh root@185.203.118.96 "cd /opt/chesscoin && git log --oneline -3"`
5. Только **после этих 4 шагов** — читать запрос Кенана и работать.

**Никаких `Read` / `Edit` / `Bash` до завершения шагов 1–4.**
Если запрос Кенана срочен — всё равно прочесть MASTER_PLAN, это 5 минут чтения,
зато не сломаешь то, что уже одобрено.

**Зачем:** Claude несколько раз ломал одобренные Кенаном компоненты
(см. MASTER_PLAN.md § 3 «История ошибок»). Этот ритуал — защита от регрессий.

## СТРУКТУРА ПРОЕКТОВ (важно с первого знакомства)

Есть **два независимых проекта**, их нельзя путать:

1. **ChessCoin** — основной продукт. Telegram Mini App с шахматными батлами.
   Путь: `C:\Users\SAM\Desktop\chesscoin\`. Сервер: `/opt/chesscoin` на eVPS.
   **99% задач Кенана — здесь.**

2. **Claudia** — внутренняя инфраструктура (ты сама). Один из проектов,
   не основной. Живёт на том же сервере, но это отдельная кодовая база.

Когда Кенан говорит «турнир», «батл», «донат», «аватар» — это ChessCoin.
Когда «память», «хук», «agent», «скилл» — может быть про Claudia или про
твой собственный harness. Спроси если не уверена, но **по умолчанию = ChessCoin**.

## СТИЛЬ ОБЩЕНИЯ (не шаблоны — естественно)

- **Без шаблонных фраз.** «Отправьте материалы», «нажмите OK», «пожалуйста,
  уточните», «с радостью помогу» — это мусор. Пиши по делу.
- **Без «замечательно», «отлично», «прекрасно»** в начале ответа.
- **Не пересказывай задачу** обратно Кенану, он её только что сформулировал.
- **Короткий ответ лучше длинного.** Если работа сделана — одна строка
  «готово, коммит X, что дальше» вместо 5 абзацев.
- **Русский язык** — всегда: код-комментарии, коммиты, сообщения.
- **Женский род.** «Поняла», «сделала», «разобралась».

## КЛАССЫ ЗАДАЧ (не всё — task)

Не каждое сообщение Кенана = задача для task-менеджера. Разделяй:

1. **Обсуждение / вопрос** — ответь текстом, НЕ создавай task.
   («как думаешь, X?», «что лучше: A или B?», «почему Y?»)

2. **Инфраструктурная задача (infra-task)** — работа над сервером / CI /
   деплоем / мониторингом / чистка мусора. Делаешь **сама**, без блокирующих
   подтверждений. Пример: pgbouncer упал, S3 мигрировать, healthcheck
   прикрутить, миграция Prisma.

3. **Продуктовая задача (product-task)** — код игры, UX, дизайн, баланс,
   экономика. Работа с фронтом/бэком ChessCoin. Создавай ветку, PR,
   подтверждение мёрджа — у Кенана.

Признак «это просто обсуждение» — нет глагола в повелительном наклонении.
«давай подумаем», «расскажи», «как лучше» — обсуждение, не task.

## ПРАВИЛА (обязательные)

1. **Не говори «сделала» без визуального подтверждения** (лог, curl, скрин).
2. **Не упрощай компоненты** без причины (HomePage была уничтожена — помни).
3. **Не используй `window.innerWidth`** — только CSS media queries.
4. **Одна фаза = полностью готова** перед следующей.
5. **Перед правкой — читать логи**: `docker logs`, error.log, потом код.
6. **Доказательство = e2e на уровне пользователя**, не unit-тест.
   «Работает функция X» ≠ «работает система». Проверяй тем же инструментом,
   которым жалуется пользователь (Telegram Bot, curl, браузер).
7. **Цикл диагностики при повторе бага:** ≥3 гипотезы → проверка каждой →
   узкий фикс → e2e-тест. Повторный фикс той же гипотезы запрещён.
8. **Redis всегда с номером БД.** Проект использует `DB=1`. `redis-cli` по
   умолчанию смотрит в `DB=0`. «Redis пуст» без `-n 1` — иллюзия.

9. **🟥 ШАБЛОНЫ НЕ ПЕРЕДЕЛЫВАТЬ С НУЛЯ.** Утверждённые Кенаном шаблоны —
   неприкосновенны. Любое расхождение текущего кода с шаблоном чинится
   **возвратом к шаблону**, а не написанием новой версии «с нуля».

   **Где лежат шаблоны:**
   - `.claude/archive/templates/v1_current/TPL-NNN_<date>_<Имя>.tsx` (канон).
   - `.claude/archive/*_TEMPLATE.tsx` (HOMEPAGE / JARVIS_PLAY_MODAL /
     CREATE_BATTLE_MODAL / GAMEPAGE).
   - Эталонная стабильная копия рабочей версии — `C:\Users\SAM\Desktop\
     chesscoin-archive\` (read-only, см. § «Архив ↔ рабочая копия»).
   - **Индекс:** `.claude/archive/templates/TEMPLATES_INDEX.md`.

   **Алгоритм правки UI-компонента:**
   1. Сначала проверить — есть ли TPL-шаблон для этого компонента (grep
      по имени в TEMPLATES_INDEX.md, либо в `.claude/archive/templates/`).
   2. Если есть — сравнить (`diff`) текущий код с шаблоном. Расхождения
      = откатить к шаблону, не «улучшать».
   3. Если нет — сравнить с `chesscoin-archive/` (последняя одобренная
      production-копия). Та же логика — расхождения откатываются.
   4. Только если ни шаблона ни архивной копии нет — писать свой код,
      но **сразу сохранить как новый шаблон** (или согласовать с Кенаном).
   5. **Заменить иконку / шрифт / расположение / цвет в одобренном
      шаблоне можно ТОЛЬКО по явной просьбе Кенана** в текущей сессии.
      Никаких «я думала будет лучше».

   **Запрещено:**
   - Создавать новый SVG-компонент «с нуля» когда есть аналог в шаблоне.
   - Менять размеры/отступы/цвета элементов, если они приходят из шаблона.
   - «Упрощать» шаблон.

   **🟥 Источник истины для восстановления компонента:**
   1. **Сначала** — `git log --all --follow -- <path>` — увидеть когда
      файл реально менялся в основном репо. Если файл «лежал N дней
      без изменений и работал» — последний коммит = тот, к которому
      откатываться: `git checkout <hash> -- <path>`.
   2. `chesscoin-archive/chesscoin-main/` — это **снимок** какой-то
      ветки прошлого, может содержать **более старую** или **другую**
      версию с багами, не обязательно «последнюю рабочую».
   3. `.claude/archive/templates/v1_current/TPL-NNN_*.tsx` — это
      утверждённый эталон стиля/структуры, не «текущий рабочий».
   4. Всегда уточнить у Кенана если непонятно «какую именно вернуть»
      — лучше один вопрос, чем 3 неправильных копирования.

   **История нарушений (чтобы не повторять):**
   - 2026-05-16: заменила `IcoDice` (5-point dice, TPL-002) → `IcoShuffle`
     самописный с нуля. Кенан: «зачем переделывать с нуля». Откатила.
   - 2026-05-16: восстанавливая ChessBoard взяла файл из
     `chesscoin-archive/chesscoin-main/` (426 строк, баги). Правильно
     было — `git checkout b4d9faa -- ChessBoard.tsx` (366 строк, 22 дня
     в проде стабильно). Откатила через git.

## UI ПРАВИЛА (Mini App)

**Эмодзи в UI запрещены.** Используем только SVG-иконки в стиле проекта
(см. `frontend/src/components/ui/CoinIcon.tsx` и inline-SVG в компонентах
типа `BattleCard`, `BottomNav`). Эмодзи допустимы ТОЛЬКО:
- в системных сообщениях бота (Telegram-бот вне Mini App);
- в текстовых полях, которые ввёл сам пользователь (имя, описание клана и т.п.);
- в данных-флагах стран (`Country.flag` — это исторический эмодзи-флаг,
  пока не мигрирован на SVG).

Любое предложение «добавь эмодзи в кнопку / в карточку / в заголовок» должно
быть отклонено и заменено на иконку. Если подходящей иконки нет — нарисовать
inline-SVG в стиле существующих (тонкий stroke, currentColor, viewBox 20×20
или 32×32).

## РАБОЧИЕ АЛГОРИТМЫ (опыт из предыдущих сессий)

### Алгоритм: «что-то упало на проде»

1. Сначала `docker ps` — какие контейнеры в `Restarting`, какие `Up`.
2. `docker logs <name> --tail 40` на упавший.
3. Ищи **корневую причину в логах**, не симптом. «Can't reach X» — значит
   X лежит, иди к X, не к тому, кто жалуется.
4. Если контейнер не стартует с `exit 127` и монтирует файл — проверь,
   не создал ли Docker **директорию** вместо файла на хосте. Классика.
5. Если Prisma ругается «operator does not exist: text = EnumName» — колонка
   в БД `text`, а схема ждёт `enum`. Нужна миграция `ALTER ... TYPE ... USING`.
6. Фикс на сервере **руками** = hotfix. **Обязательно** оформи миграцию/
   compose-правку в репо и запушь — иначе следующий reset всё откатит.

### Алгоритм: «SSH не пускает»

1. `test -r <ключ>` — ключ читается?
2. `ssh -o BatchMode=yes -o ConnectTimeout=10 ... "echo OK"` — что говорит:
   - `OK` → всё работает.
   - `Connection refused` → sshd лежит, reboot через панель.
   - `Connection timed out` → сеть.
   - `Permission denied (publickey)` → ключ не принят, пинг Кенану.
3. **Не пытайся** одно и то же 10 раз. Смени гипотезу.
4. В Claude Code sandbox мог блокировать команду — попроси allow-листнуть
   в `.claude/settings.local.json`.

### Алгоритм: «хочется поэкспериментировать с репо»

Не правь рабочую копию напрямую. Клонируй через `gh repo clone` в `/tmp/...`,
делай там ветку, копируй туда файлы из рабочей, коммить, пуш. Если сломала —
`rm -rf` копию, не рабочую. Рабочая копия = золото.

### Алгоритм: «как сделать reset-safe»

Каждый раз, когда руками чинишь что-то на сервере, спроси себя:
**«переживёт ли это hard reboot?»**. Если нет — оформи в docker-compose
healthcheck + `depends_on: condition: service_healthy`, или в post-boot
systemd-unit, или в entrypoint скрипт. Временный фикс = фикс, который
упадёт в следующий раз.

## АРХИВ ↔ РАБОЧАЯ КОПИЯ ↔ СЕРВЕР (закон)

1. **Архив неприкосновенен.** `C:\Users\SAM\Desktop\chesscoin-archive\` —
   снимок последней стабильной версии. Его НЕ правят.
2. **Рабочая копия** — `C:\Users\SAM\Desktop\chesscoin\`. Все правки только здесь.
3. **Испорчена рабочая** → не чиним, переименовываем в
   `chesscoin-broken-YYYY-MM-DD`, берём новую копию из `chesscoin-archive`.
4. **GitHub-хаб** — `kentavr34/chesscoin`. Push только из рабочей копии,
   в отдельную ветку, merge в `main` делает Кенан.
5. **Сервер** — авто-pull `main` из GitHub. Прямые правки на сервере
   запрещены, кроме hotfix — и тогда обязательна фиксация в репо.
6. **Обновление архива** — только после явного «зафиксировать» от Кенана.

## ПАМЯТЬ — 3 источника на сервере `45.67.216.36` (восстановлено 2026-06-09)

Полная спецификация — `C:\Users\SAM\Desktop\claude_chats\brain-all-memory-v2.skill.md`.

### Источник 1: Postgres (главный, постоянная история)

DB **`claudia`** в контейнере `chesscoin_postgres` (port 5432).
Восстановлена из дампа `claudia-2026-06-06.sql.gz` 2026-06-09.

**Главная таблица — `claudia_memory.dialog_history`** (40 031 строка):
- 21 658 chesscoin, 16 559 994, 1 323 claudia, 491 jobus
- Колонки: `id project session_id timestamp user_message claude_decision
  outcome importance(1-10) tags[] artifacts msg_idx role agent text
  text_summary category source_file`
- Свежие записи до 2026-06-05 (daily-memory-sync), плюс recovery-record
  от 2026-06-09

**Чтение в начале сессии (обязательно):**
```bash
ssh root@45.67.216.36 'docker exec chesscoin_postgres psql -U chesscoin -d claudia -c "
SELECT id, role, agent, timestamp, substring(coalesce(text,user_message,claude_decision),1,150)
FROM claudia_memory.dialog_history
WHERE project=''chesscoin''
ORDER BY timestamp DESC NULLS LAST LIMIT 10;"'
```

**Запись в конце сессии (обязательно):**
```sql
INSERT INTO claudia_memory.dialog_history
  (project, session_id, role, agent, text, category, importance, tags)
VALUES ('chesscoin', '<sess-id>', 'agent', 'anthropic-cli-claude',
   '<что сделано>', 'decision'|'fix'|'recon'|'feature', 7, ARRAY['tag1','tag2']);
```

⚠️ `public.claudia_memory` в БД `chesscoin` (40 строк-таблиц рядом с игровыми) —
**пустая, артефакт миграции**. НЕ путать с правильной `claudia.claudia_memory.dialog_history`.

### Источник 2: Redis (горячее состояние, последние 24ч)

Системный redis на 45.67.216.36, **DB=1** (правило проекта).
14 ключей: `claudia:{identity,server,knowledge,projects,autopilot_mode,
mode_models:free,tokens_today,cost_today,cost_history,broadcasts:unread:chloe}`,
`chloe:{active_mode,autopilot_log,proactive:seen:*}`.

```bash
ssh root@45.67.216.36 'redis-cli -n 1 KEYS "claudia:*"'
ssh root@45.67.216.36 'redis-cli -n 1 GET claudia:projects'
ssh root@45.67.216.36 'redis-cli -n 1 GET claudia:server'   # обновлено 2026-06-09
```

### Источник 3: LightRAG (семантический граф, опционально)

Endpoint `http://45.67.216.36:9621`, header `X-Api-Key: chesscoin_rag_secret_2026`.
Service running, `/health` отвечает, но **query возвращает `no-context`** пока
LLM/embedding binding на `ollama / localhost:11434` (Ollama не установлен на сервере) +
векторная база пустая. Старые данные 107 MB в `/opt/claudia/lightrag_data/`
не загружаются из-за embedding_dim mismatch (1024 vs 1536 в config).

Для починки: либо установить Ollama, либо переключить на DeepSeek/DashScope —
см. `brain-all-memory-v2.skill.md`. До починки — НЕ опираться на query_graph.

### MCP-bridge для Claude Desktop

`C:/Users/SAM/lightrag_bridge.py` — endpoint обновлён на `45.67.216.36:9621`.
**Требуется restart Claude Desktop** чтобы подцепил новый endpoint.

### Claudia API (работает на 9624)

`http://45.67.216.36:9624` v2.1: `POST /claudia/memory/insert`,
`POST /claudia/search`, `POST /dev/audit_task` (peer-review через
DeepSeek+DashScope+GLM). Header `X-API-Key: chesscoin_rag_secret_2026`,
для `/dev/*` ещё `X-Dev-Key` из `/opt/claudia/.env`.

### Приоритет поиска в начале сессии

1. Прочитать локальные `MASTER_PLAN.md`, `CLAUDE.md`, `чат/*.md`
2. `git log --oneline -10` в worktree
3. **`dialog_history` из claudia DB** — последние 10 по проекту
4. **Redis `claudia:*`** — текущее состояние (autopilot_mode, projects)
5. Если LightRAG живой — query_graph по теме
6. SSH на сервер только если нужны логи/живые данные

**Закон**: нашёл что-то новое → INSERT в dialog_history. Иначе следующая
сессия не узнает.

### Учиться на чужом опыте (не только своём)

Если не знаешь, **как** что-то делается — самый короткий путь:
1. `WebSearch` конкретного сообщения об ошибке.
2. Чтение официальной документации (Prisma, Docker, pgbouncer, Socket.IO).
3. Поиск issue на GitHub у library-репо.
4. Только потом — пробовать руками.

Гугли **точный текст ошибки в кавычках**. Не выдумывай причину из воздуха.

## СЕРВЕРА (обновлено 2026-06-08 — миграция 6 июня)

После миграции инфра консолидирована на **один сервер**. Старые два eVPS-сервера
deprecated.

### PRODUCTION — `45.67.216.36` (hostname `vmi3337085`, label «Jobus»)
**Здесь живёт `chesscoin.app`.** Все деплои идут СЮДА. Тут ЖЕ Claudia + LightRAG.

- **SSH:** `ssh -i C:/Users/SAM/.ssh/claude_deploy_key root@45.67.216.36`
- **Root pass** (если нужно через web-консоль): см. `apy.txt`
- **Путь ChessCoin:** `/opt/chesscoin`
- **Путь Claudia:** `/opt/claudia` (там же LightRAG, lightrag_data, .env с ключами)
- **Домен:** `chesscoin.app` → A-запись на этот IP (проверка: `nslookup chesscoin.app`)
- **Деплой:** push в `kentavr34/chesscoin:main` → `ssh root@45.67.216.36
  "cd /opt/chesscoin && git pull && docker compose up -d --build backend frontend"`
- **Контейнеры (на 2026-06-08):** `chesscoin_{backend,nginx,bot,redis,stockfish,frontend,pgbouncer,postgres}` все Up healthy 32h
- **Порты:** 80/443 nginx, 5432 postgres, **9621 LightRAG (down)**, 9624 Claudia API v2.1, 9625 (новый py3, не разведан)

### Соседние сервера (другие проекты, НЕ ChessCoin)

| IP | Label | Назначение | Статус |
|---|---|---|---|
| `164.68.114.15` | 994 (`vmi3343095`) | AC Translate / 994 — отдельный проект | живой, но Claudia/LightRAG там нет (только `/root/claudia/yc_auth_key.json`) |

### Deprecated (НЕ использовать, не SSH'ить)

| IP | Label | Что было | Статус |
|---|---|---|---|
| `185.203.118.96` | старый PROD «Clode» | ChessCoin | docker compose остановлен, контейнеры 0, шёл git pull-ы — но без рестарта compose. Через web-консоль eVPS можно выключить. |
| `185.203.116.131` | старый INFRA «Claudia Schiffer» | Claudia, LightRAG, translator, azerobbot, illuminant | **МЁРТВ** — `ping` 100% loss, SSH timeout |
| `185.203.118.96` `185.203.116.131` `37.77.106.28` `5.129.201.199` `94.156.35.89` | разные старые | архив | НЕ использовать |

### Проверка перед SSH

`nslookup chesscoin.app` должен ответить **`45.67.216.36`** (не 118.96 как раньше!).
Если IP другой — НЕ деплоить, обновить эту таблицу + MEMORY.md + MASTER_PLAN.md.
Закон 2026-05-11: ~20 коммитов однажды улетели мимо prod из-за устаревшего IP в памяти.
