# CLAUDE.md — ChessCoin

## ПРАВИЛА (обязательные)
1. **Язык: ТОЛЬКО русский** — комментарии, коммиты, сообщения
2. **Не говори "сделал" без визуального подтверждения**
3. **Не упрощай компоненты** без причины (HomePage была уничтожена — помни)
4. **Не используй window.innerWidth** — только CSS media queries
5. **Одна фаза = полностью готова** перед следующей
6. **Клаудия — женского рода**: «Поняла», «приняла», «готова» (не «Понял»)
7. **Перед изменениями — читать логи**: сначала error.log / docker logs, потом код

## АРХИВ ↔ РАБОЧАЯ КОПИЯ ↔ СЕРВЕР (закон)

1. **Архив неприкосновенен.** `C:\Users\SAM\Desktop\chesscoin-archive\` — снимок последней стабильной версии (коммит зафиксирован в `_ARCHIVE_README.md`). Его НЕ правят.
2. **Рабочая копия** — `C:\Users\SAM\Desktop\chesscoin\`. Все правки только здесь.
3. **Испорчена рабочая** → не чиним, переименовываем в `chesscoin-broken-YYYY-MM-DD`, берём новую копию из `chesscoin-archive`.
4. **GitHub-хаб** — `kentavr34/chesscoin`. Push только из рабочей копии, в отдельную ветку, merge в `main` делает Кенан.
5. **Сервер** — авто-pull `main` из GitHub (GitHub Action / webhook). Прямые правки на сервере запрещены.
6. **Обновление архива** — только после явного «зафиксировать» от Кенана: старый архив → `chesscoin-archive-YYYY-MM-DD`, новый снимок рабочей → новый `chesscoin-archive`.

## ПАМЯТЬ
Весь контекст, история сессий, задачи, дизайн-стандарты — в **LightRAG**:
- MCP инструмент: `query_graph("вопрос")` / `memory_search` — запросить
- MCP инструмент: `save_to_memory("текст")` / `memory_remember` — сохранить
- Веб-интерфейс: https://chesscoin.app/webui/

**Приоритет поиска:**
1. Сначала — `memory_search` / `query_graph` (LightRAG).
2. Не нашла → локальные файлы (README, `.claude/management/`, CLAUDE.md).
3. В последнюю очередь — сервер (SSH).

**RAG-цикл:** Нашла в локальных файлах или через SSH — **обязательно** `save_to_memory()`, чтобы в следующей сессии не искать снова.

## СЕРВЕР
- **Хост:** `185.203.116.131` (ChessCoin + Claudia + LightRAG, один сервер).
- **SSH:** `ssh -i C:/Users/SAM/.ssh/claude_deploy_key root@185.203.116.131`
- **Путь:** `/opt/chesscoin`
- **Ветка Клаудии:** `claude/blissful-jones`
